# Agent 问答 Bad Case 记录

> 更新时间：2026-09-09
> 范围：近期盘前推荐与 Tool-Using Agent 问答中已经实际暴露的问题。本文记录错误表现、根因、修复方案、验收方式及剩余风险，供后续评测与回归使用。

## BC-001：股票列表问题被错误回答成历史统计

### 用户问题

> 近期涨停后回撤比较多的股票有哪些？

### 错误表现

Agent 没有直接返回“盘前推荐 → 高位回撤”中的当前股票候选，而是进入了涨停后历史统计或泛化研究流程，回答内容与用户要的“有哪些股票”不一致。

### 根因

1. Planner 把“涨停后回撤”识别成了涨停后形态研究，但没有进一步区分“列出当前候选”和“统计历史表现”两种任务形态。
2. 系统过度依赖 LLM 选择工具，缺少对“哪些、有哪些、列出”等列表型语义的确定性校正。
3. `post_limit_statistics` 与 `post_limit_screen` 的能力边界在工具描述和回答模板中不够清楚，导致 Planner 选错工具后仍能生成表面通顺、实际答非所问的回答。

### 解决方案

1. 将“近期涨停后回撤比较多的股票有哪些”一类问题路由到 `post_limit_screen`，直接复用盘前推荐的高位回撤筛选结果。
2. 对包含“哪些、有哪些、名单、股票”等列表意图的问题增加语义校正：即使 Planner 选成统计工具，也要在执行前纠正为筛选工具。
3. 回答模板直接展示当前候选、实际筛选窗口和关键条件，避免再生成无关的历史统计结论。
4. 相关修复提交：`7209e17 fix: route drawdown list questions to premarket screen`。

### 验收与防回归

- 使用上述原始问法做固定回归用例。
- 断言实际调用 `post_limit_screen`，形态为高位回撤。
- 断言回答中的股票集合与盘前推荐页面一致。
- 断言回答显示 Query Contract 中的实际窗口，不能由模板写死为 5 日或 7 日。

---

## BC-002：回答中的股票名称无法点击跳转

### 用户问题

用户在 Agent 回答中看到股票名称，但点击名称无法进入个股详情页。

### 错误表现

回答文字中虽然出现了股票名称，前端却只能把它当普通文本渲染，不能生成站内个股链接。

### 根因

1. 前端生成股票链接需要稳定的结构化身份信息，例如股票代码与名称的对应关系。
2. 涨停后筛选工具在 trace/facts 中只保留了摘要或被截断的候选信息，完整的 `symbol + name` 没有传递到前端。
3. 单纯从自然语言答案中反向猜测股票代码不可靠，名称还可能存在同名、简称变化和文本误匹配，因此不能只靠前端正则补救。

### 解决方案

1. 在涨停后筛选结果的结构化 trace/facts 中保留全部候选的股票代码和名称。
2. 前端继续基于结构化股票身份生成链接，不从自然语言文本中猜代码。
3. 相关修复提交：`50ec6fa fix: preserve post-limit stock links in agent answers`。

### 验收与防回归

- 对多只股票的回答检查 trace 中是否包含完整的 `symbol + name`。
- 检查每个候选名称都能跳转到对应个股详情页。
- 增加候选数量超过摘要截断上限时的测试，确保尾部股票仍然可点击。

---

## BC-003：已有涨停数据，但 Agent 无法统计近期热门板块

### 用户问题

> 近期哪些板块涨停的股票比较多？

### 错误表现

Agent 返回无法回答，或者只查询了单日涨停明细，没有完成“近期、多交易日、按板块聚合”的统计。

### 根因

1. 早期 `limit_up_events` 工具只支持查询明细，缺少“最近 N 个交易日”和 `group_by` 聚合能力。
2. Query Contract 的主题提取过于激进，把“近期哪些”误当成搜索关键词，最终得到 `query="近期哪些"` 和零匹配结果。
3. Planner 即使理解了自然语言，也只能在已有工具契约内工作；当工具只能返回明细且数据量受限时，LLM 无法可靠完成全量、可审计的跨日统计。
4. 当时服务进程还存在版本滞后问题，健康检查成功，但真实 Agent 请求仍由旧版 Query Contract 处理，进一步掩盖了修复是否生效。

### 解决方案

1. 将 `limit_up_events` Query Contract 升级，增加 `recent_trade_days` 与 `group_by`。
2. 在后端确定性完成跨交易日聚合，同时返回去重股票数和涨停事件数；不能把大量原始明细截断后交给 LLM 心算。
3. 对“近期哪些板块涨停的股票比较多”增加语义护栏，默认按最近 7 个交易日聚合；用户明确“近 N 个交易日”时使用 N。
4. 修正泛化主题词提取，避免把“近期哪些、哪些板块”等问句骨架当成筛选关键词。
5. 增加确定性回答模板和降级路径，即使 LLM 生成失败也能基于工具结果回答。
6. 相关修复提交：`a24f4ac fix(agent): aggregate recent limit-up sectors`。

### 验收与防回归

- 固定回归原始问法，断言契约包含 `recent_trade_days=7` 和聚合维度。
- 显式询问“近 10 个交易日”时，断言 10 覆盖默认 7。
- 断言统计基于完整工具结果，而不是仅使用显示层截断后的前若干条明细。
- 同时检查去重股票数和事件数，避免同一股票多日涨停造成口径误解。
- 通过真实 `/api/agent/...` HTTP 请求检查 `query_contract.version`，不能只看 `/health`。

---

## BC-004：“智慧农业”被归入“汽车零部”板块

### 用户反馈

> 你在搞什么鬼，智慧农业怎么成汽车零部行业了？

### 错误表现

在回答“近期哪些板块涨停的股票比较多”时，系统按行业字段聚合，出现了“汽车零部：智慧农业”的结果。这个展示不仅违背用户对“板块”的理解，也会让人误以为是 Agent 随意编造分类。

### 根因

1. Query Contract v3 把用户说的“板块”直接等同于 `industry`，忽略了 A 股语境中“板块”往往指题材、概念或涨停原因，而不只是申万/同花顺行业。
2. 本地涨停池中的行业字段来自上游涨停池数据；对应日期的原始数据本身把智慧农业标记为“汽车零部”。聚合代码没有串错股票，但直接沿用了一个存在口径差异或时效问题的上游标签。
3. 交叉核验显示，同花顺涨停原因更接近“有色金属、锌铜矿、制造业、智慧农业”等题材信息，当前行业成分也不支持将其简单解释为汽车零部。
4. 回答没有展示分类来源和口径，使“上游标签冲突”看起来像“模型事实错误”。

### 解决方案

1. 将 Query Contract 升级到 v4，明确区分：
   - 用户说“板块、题材、概念”时，按涨停原因/题材聚合；
   - 用户明确说“行业”时，才按行业字段聚合。
2. 将涨停原因按 `+`、`、`、逗号、分号、竖线等分隔符拆成多个题材标签，再进行确定性统计。
3. 允许一只股票属于多个题材，并在回答中明确这是“题材覆盖统计”，各题材数量不能简单相加。
4. 增加智慧农业专门回归用例：相关板块回答应体现其涨停题材，不能再次展示为“汽车零部”。
5. 相关修复提交：`af4ea1b fix(agent): classify sector queries by limit-up themes`。

### 验收与防回归

- 用原始问题进行真实 HTTP 回归，断言 `group_by=concept` 或等价题材维度。
- 结果中不得出现“汽车零部：智慧农业”。
- 明确询问“近期哪些行业涨停股票多”时，仍允许走行业聚合，但回答必须标明行业字段的数据来源。
- 对“板块”和“行业”分别建立测试，防止后续提示词或 Planner 调整又把两者合并。

### 剩余风险

- 用户明确询问“行业”时，目前仍会使用上游行业字段；不同数据源的行业体系、更新时间可能不一致。
- 涨停原因属于多标签数据，同一股票可能同时计入多个题材，必须持续在接口和展示层标明统计口径。
- 某些历史涨停事件可能缺少涨停原因；缺失项应进入 `data_missing` 或覆盖率说明，不能被当成“无题材”。

---

## BC-005：代码已经修改，但线上问答仍表现为旧版本

### 用户反馈

> 你重启服务了嘛，怎么我问 Agent，它还是回答不了？

### 错误表现

代码和测试已经包含新能力，但用户实际访问的 Agent 仍返回旧版结果。仅看健康检查会误判“服务已经更新”。

### 根因

1. 旧 Uvicorn 进程仍占用端口，新启动命令没有真正替换提供请求的进程。
2. `/health` 只能说明端口上有服务，不能证明正在运行的是本次代码版本。
3. 重启后的验收没有发送用户原始问题，也没有检查回答 trace 中的 Query Contract 版本和参数。
4. 后台启动方式的进程生命周期不稳定，启动命令返回成功不等于新服务持续存活并接管流量。

### 解决方案

1. 重启前定位监听端口的准确 PID，停止旧进程后再启动新服务。
2. 使用可持续的本地服务会话，确认进程没有在启动命令结束后退出。
3. 重启验收从“进程/健康检查”升级为端到端验证：发送用户原始问题，检查响应内容、工具名、Query Contract 版本和关键参数。
4. 通过持久化的 Agent run/trace 反查真实请求，区分“代码逻辑错误”和“旧进程仍在服务”。

### 验收与防回归

- 记录当前监听 PID、服务启动时间和代码版本。
- `/health` 成功后必须再发送至少一条真实 Agent 请求。
- 对本次案例，断言实际响应使用新版契约，并能返回近期板块/题材统计。
- 如果响应仍显示旧契约版本，应立即判定重启失败，不能向用户宣称已生效。

---

## BC-006：指定板块的近期涨停股票名单被解析为空结果

### 用户问题

> 近期农业板块涨停过的股票有哪些？

### 错误表现

Agent 选择了涨停事件工具，但最终回答“抱歉，该问题无法回答”。实际 trace 显示查询词为“近期农业”、窗口为 1 个交易日，因此本地数据零命中。

### 根因

1. 现有语义规则只覆盖“哪些板块涨停股票比较多”的聚合问题，没有覆盖“指定一个板块，再反向列出股票”的任务形态。
2. 主题提取正则把时间修饰词“近期”一起捕获，错误生成 `query=近期农业`。
3. 非聚合查询沿用了 Planner 给出的单日窗口，没有把“近期”编译为统一的 7 个交易日。
4. 普通列表仍交给 LLM 组织答案，缺少针对跨日股票名单的确定性去重展示。

### 解决方案

1. 新增“指定板块/题材/行业的近期涨停股票名单”语义识别，并强制使用 `limit_up_events`。
2. 清理主题词前的“近期、最近、近 N 个交易日、今天”等时间修饰词，原问题稳定提取为 `query=农业`。
3. “近期”默认查询最近 7 个交易日，显式“近 N 个交易日”仍以 N 为准；“今天”保持单日。
4. Query Contract 升级为 `limit-up-query-v5`，避免 Planner 将上述确定性参数改回错误值。
5. 跨日名单按股票代码去重，同时展示期间涨停次数、日期和涨停题材。
6. 板块/题材问法不再附带存在来源冲突的行业字段，避免再次出现“智慧农业显示汽车零部”的误导。

### 验收与防回归

- 固定回归原始问法，断言 `query=农业`、`recent_trade_days=7`、`result_mode=list`。
- 增加“今天农业板块涨停的股票有哪些”，断言保持 1 个交易日。
- 增加“近 10 个交易日农业板块涨停过的股票有哪些”，断言显式窗口覆盖默认值。
- 使用真实本地接口验证返回亚盛集团、智慧农业和中基健康，重复涨停股票只列一次。
- 断言板块回答中不再出现“汽车零部：智慧农业”等冲突行业展示。

---

## BC-007：晋级股票的开盘表现追问被误判为交易时间

### 用户连续问题

> 最近两天，一进二成功的票有哪些？
>
> 这些票在1-2晋级成功的那天，开盘的情况是什么样，高开还是低开的多？

### 错误表现

第一问正确列出最近两个晋级日的一进二股票；第二问没有分析这些股票，反而回答 A 股几点开盘、集合竞价时间等交易制度说明。

### 根因

1. 前端和后端的确定性意图规则只要看到“开盘”就识别为 `market_schedule`，没有区分“几点开盘”和“股票开盘价表现”；前端还会把错误的 `intent_hint` 传给后端。
2. 后端曾无条件信任前端 `intent_hint`，而交易时间回答又位于 LLM Planner 之前，误判后会直接返回静态答案，LLM 和行情工具均没有纠错机会。
3. 会话上下文只保存通用能力和部分股票信息，没有保存上一轮晋级查询的实际交易日窗口，追问中的“这些票”无法稳定映射回同一批样本。
4. 原晋级统计只有晋级日期、板数和股票名单，没有晋级日开盘价相对前收的确定性指标，LLM 即使理解问题也缺少可审计事实。

### 解决方案

1. 同时收紧前后端交易时间意图：仅“几点开盘、是否开盘、交易时间、集合竞价时间”等问法进入静态回答；“高开、低开、平开、开盘价、开盘情况、开盘表现”明确排除。后端还会拒绝与原始问句不一致的 `market_schedule` 提示，避免旧前端或其他客户端绕过语义校验。
2. 将上一轮 `daily_board_promotion` 的 `days` 写入结构化会话上下文；追问未指定窗口时沿用上一轮，用户显式指定新窗口时再覆盖。
3. Planner 选择晋级工具但擅自给出其他窗口时，由后端规范化规则强制恢复实际上下文窗口。
4. 使用本地 `stock_daily_bars` 批量读取晋级日开盘价和前一交易日收盘价，确定性计算 `open_gap_pct`，并分类为高开、平开、低开。
5. 只对 `1→2` 晋级样本汇总高低开数量；缺少任一对应日 K 线时显式列入缺失，不当作平开或零值。
6. 对该类追问使用确定性完整模板，按晋级日列出全部股票和开盘涨跌幅，并给出高开与低开数量比较；股票代码继续进入结构化 mention，支持详情跳转。

### 验收与防回归

- 固定保存上述两轮原话，断言第二轮意图不是 `market_schedule`，实际调用晋级统计且沿用第一轮 `days=2`。
- 构造一只高开和一只低开的跨日晋级样本，断言开盘涨跌幅分别为 `+5.00%` 和 `-5.00%`，汇总为数量相同。
- 单独验证“A 股几点开盘”仍返回交易时间说明，避免修复分析问法时破坏原能力。
- 使用真实本地数据核验 2026-09-08 与 2026-09-09：22 只一进二样本均可评价，高开 20 只、平开 1 只、低开 1 只。

---

## 共性问题总结

近期 Bad Case 不是单一的“LLM 意图识别不够聪明”，而是以下几类系统问题叠加：

1. **意图与任务形态未分层**：识别到“高位回撤”还不够，还要区分列表、统计、解释和比较。
2. **工具契约能力不足**：系统有原始数据，不代表 Agent 自动具备跨日聚合、去重和分类统计能力；可审计计算需要由后端工具完成。
3. **业务词口径不明确**：“板块”不应未经判断就等同于“行业”，题材、概念、行业和涨停原因必须分别定义。
4. **证据链在展示层丢失**：自然语言中有股票名称，但缺少结构化代码，最终导致链接能力失效。
5. **数据源标签存在冲突**：上游返回的行业或题材也可能过时或口径不同，系统需要标明来源、做交叉核验并显式暴露缺失。
6. **部署验收不完整**：健康检查不能替代真实业务请求；版本化 Query Contract 应成为判断新逻辑是否生效的依据。

## 后续统一验收基线

每次修改 Agent 工具、Query Contract、Planner 或回答模板后，至少完成以下检查：

1. 保存用户真实失败问法，作为固定回归用例，不只测试人工改写后的标准问法。
2. 检查 Planner 结果、最终实际工具、Query Contract 和回答四层是否一致。
3. 聚合、去重、筛选等事实计算在确定性代码中完成，LLM 只负责规划、解释和总结。
4. 回答中的股票、日期、窗口、分组口径和数据缺失均能在 trace/facts 中找到依据。
5. 页面已有能力被 Agent 复用时，验证两端候选集合与筛选口径一致。
6. 服务重启后用真实 HTTP 请求验证用户原始问题，并核对契约版本。
7. 对数据源冲突保留专项样本，例如智慧农业，防止同类错误再次出现。
