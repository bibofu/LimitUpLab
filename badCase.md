# Agent 问答 Bad Case 记录

## BC-027：任务运行时数字引用格式异常导致整答失败（2026-09-12）

- 来源：冻结真实模型初验 `live-20260912T070910Z-fd93f2a5` 的市场全景、指数和评级简单题。
- 原因：模型给出 `indices[0].value`，字段访问器只接受点号；异常越过任务段落边界，导致全部回答丢失。
- 修正：有限支持整数/通配符括号路径，继续禁止表达式执行；无效声明仅使对应段落降级；按任务保留 dict/list 原始证据。
- 单测覆盖有限路径、批量预算耗尽仍保留成功实体、未执行任务不可被模型自评通过。冻结简单题复测6×1为6/6、6×3为17/18；唯一失败为Provider错误，尚未达到稳定性门槛。
- 相关旧十题的任务作用域问题通过新运行时方向性改造处理，尚未宣称逐题修复；默认 legacy 不变。

## BC-028：冻结工具忽略显式筛选导致无效批量展开（2026-09-12）

- 现象：`highest_only=true` 仍返回全部涨停股票，后续龙虎榜/K线被展开到8只并耗尽工具预算。
- 原因：完全冻结工具适配层返回固定 payload，但未按 `board_height`、`min_board_height`、`highest_only`、市场、query、limit 应用确定性筛选。
- 修正：冻结适配层在返回前应用同一组显式参数并重算 matched/unique/returned count；增加最高板只能返回美能能源(001299、3板)的回归。
- 限制：这修复的是冻结工具世界的参数语义，不代表复杂题已通过；8×3实际仅15/24，稳定3/8，默认路径不切换。

## BC-029：条件分支被重复补查且全量 empty 被误判（2026-09-12）

- 现象：最终提交 `6f4eea3` 的复杂冻结验收只有10/24；互斥分支已由条件判定不适用，仍被本地覆盖检查标为缺失并反复Replan；全量龙虎榜返回empty时，Evaluator仍要求逐实体query参数而误判依赖失败。
- 原因：覆盖检查只把ok/empty步骤当作证据，没有保存“条件未命中”的观测；参数依赖提取把缺失参数读成`None`值，且未表达无过滤完整empty结果的集合语义；`source_text`的严格字符串包含检查也会因模型轻微改写废弃整张计划。
- 修正：保存条件来源与匹配结果，未触发分支可由该观测闭合；绑定路径错误可覆盖模型的错误不可恢复判断并进入一次受限Replan；原始问题作为不匹配`source_text`的安全溯源回退；Evaluator接受无过滤且结果状态为empty的全集反证；用户原始日期/TopN/窗口数字不要求伪造市场claim。
- 验收：后续正式复杂8×3提升至18/24，能力/工具召回100%、无效Replan为0，但仅4/8达到3/3稳定，仍未通过发布门槛，默认保持legacy。

## BC-030：历史问题混入当前热榜与新闻（2026-09-12）

- 现象：真实HTTP询问2026-05-15的人气Top2、K线和条件新闻时，当前快照热榜与2026-09新闻被拼入5月K线答案，形成跨日期事实污染。
- 原因：工具Schema只有参数类型，没有声明`historical`或`snapshot_only`时间能力；Planner无法识别无日期参数的当前榜单/新闻工具不能回答历史截面，运行时也没有最终门禁。
- 修正：运行时工具Schema增加时间契约和快照日期；执行前从原始问题及Requirement约束提取显式日期，生产环境的`snapshot_only`工具不得服务其他日期；冻结评测工具仍按冻结快照日期执行。时间门禁失败保留为任务级partial，不调用当前数据源。
- 验收：新增历史请求不得调用当前快照工具的回归，最终定向48项通过。按用户要求停止后未再跑Live或真实HTTP，默认仍为legacy。

> 更新时间：2026-09-09
> 范围：近期盘前推荐与 Tool-Using Agent 问答中已经实际暴露的问题。本文记录错误表现、根因、修复方案、验收方式及剩余风险，供后续评测与回归使用。

## BC-001：股票列表问题被错误回答成历史统计

### 用户问题

> 近期涨停后回撤比较多的股票有哪些？

### 错误表现

Agent 没有直接返回“盘前推荐 → 高位回撤”中的当前股票候选，而是进入了涨停后历史统计或泛化研究流程，回答内容与用户要的“有哪些股票”不一致。

### 根因

1. Planner 把“涨停后回撤”识别成了涨停后形态研究，但没有进一步区分“列出当前候选”和“统计历史表现”两种任务形态。
2. 系统过度依赖 LLM 选择工具，缺少对“哪些、有哪些、列出”等列表型语义的确定性校正。
3. `post_limit_statistics` 与 `post_limit_screen` 的能力边界在工具描述和回答模板中不够清楚，导致 Planner 选错工具后仍能生成表面通顺、实际答非所问的回答。

### 解决方案

1. 将“近期涨停后回撤比较多的股票有哪些”一类问题路由到 `post_limit_screen`，直接复用盘前推荐的高位回撤筛选结果。
2. 对包含“哪些、有哪些、名单、股票”等列表意图的问题增加语义校正：即使 Planner 选成统计工具，也要在执行前纠正为筛选工具。
3. 回答模板直接展示当前候选、实际筛选窗口和关键条件，避免再生成无关的历史统计结论。
4. 相关修复提交：`7209e17 fix: route drawdown list questions to premarket screen`。

### 验收与防回归

- 使用上述原始问法做固定回归用例。
- 断言实际调用 `post_limit_screen`，形态为高位回撤。
- 断言回答中的股票集合与盘前推荐页面一致。
- 断言回答显示 Query Contract 中的实际窗口，不能由模板写死为 5 日或 7 日。

---

## BC-002：回答中的股票名称无法点击跳转

### 用户问题

用户在 Agent 回答中看到股票名称，但点击名称无法进入个股详情页。

### 错误表现

回答文字中虽然出现了股票名称，前端却只能把它当普通文本渲染，不能生成站内个股链接。

### 根因

1. 前端生成股票链接需要稳定的结构化身份信息，例如股票代码与名称的对应关系。
2. 涨停后筛选工具在 trace/facts 中只保留了摘要或被截断的候选信息，完整的 `symbol + name` 没有传递到前端。
3. 单纯从自然语言答案中反向猜测股票代码不可靠，名称还可能存在同名、简称变化和文本误匹配，因此不能只靠前端正则补救。

### 解决方案

1. 在涨停后筛选结果的结构化 trace/facts 中保留全部候选的股票代码和名称。
2. 前端继续基于结构化股票身份生成链接，不从自然语言文本中猜代码。
3. 相关修复提交：`50ec6fa fix: preserve post-limit stock links in agent answers`。

### 验收与防回归

- 对多只股票的回答检查 trace 中是否包含完整的 `symbol + name`。
- 检查每个候选名称都能跳转到对应个股详情页。
- 增加候选数量超过摘要截断上限时的测试，确保尾部股票仍然可点击。

---

## BC-003：已有涨停数据，但 Agent 无法统计近期热门板块

### 用户问题

> 近期哪些板块涨停的股票比较多？

### 错误表现

Agent 返回无法回答，或者只查询了单日涨停明细，没有完成“近期、多交易日、按板块聚合”的统计。

### 根因

1. 早期 `limit_up_events` 工具只支持查询明细，缺少“最近 N 个交易日”和 `group_by` 聚合能力。
2. Query Contract 的主题提取过于激进，把“近期哪些”误当成搜索关键词，最终得到 `query="近期哪些"` 和零匹配结果。
3. Planner 即使理解了自然语言，也只能在已有工具契约内工作；当工具只能返回明细且数据量受限时，LLM 无法可靠完成全量、可审计的跨日统计。
4. 当时服务进程还存在版本滞后问题，健康检查成功，但真实 Agent 请求仍由旧版 Query Contract 处理，进一步掩盖了修复是否生效。

### 解决方案

1. 将 `limit_up_events` Query Contract 升级，增加 `recent_trade_days` 与 `group_by`。
2. 在后端确定性完成跨交易日聚合，同时返回去重股票数和涨停事件数；不能把大量原始明细截断后交给 LLM 心算。
3. 对“近期哪些板块涨停的股票比较多”增加语义护栏，默认按最近 7 个交易日聚合；用户明确“近 N 个交易日”时使用 N。
4. 修正泛化主题词提取，避免把“近期哪些、哪些板块”等问句骨架当成筛选关键词。
5. 增加确定性回答模板和降级路径，即使 LLM 生成失败也能基于工具结果回答。
6. 相关修复提交：`a24f4ac fix(agent): aggregate recent limit-up sectors`。

### 验收与防回归

- 固定回归原始问法，断言契约包含 `recent_trade_days=7` 和聚合维度。
- 显式询问“近 10 个交易日”时，断言 10 覆盖默认 7。
- 断言统计基于完整工具结果，而不是仅使用显示层截断后的前若干条明细。
- 同时检查去重股票数和事件数，避免同一股票多日涨停造成口径误解。
- 通过真实 `/api/agent/...` HTTP 请求检查 `query_contract.version`，不能只看 `/health`。

---

## BC-004：“智慧农业”被归入“汽车零部”板块

### 用户反馈

> 你在搞什么鬼，智慧农业怎么成汽车零部行业了？

### 错误表现

在回答“近期哪些板块涨停的股票比较多”时，系统按行业字段聚合，出现了“汽车零部：智慧农业”的结果。这个展示不仅违背用户对“板块”的理解，也会让人误以为是 Agent 随意编造分类。

### 根因

1. Query Contract v3 把用户说的“板块”直接等同于 `industry`，忽略了 A 股语境中“板块”往往指题材、概念或涨停原因，而不只是申万/同花顺行业。
2. 本地涨停池中的行业字段来自上游涨停池数据；对应日期的原始数据本身把智慧农业标记为“汽车零部”。聚合代码没有串错股票，但直接沿用了一个存在口径差异或时效问题的上游标签。
3. 交叉核验显示，同花顺涨停原因更接近“有色金属、锌铜矿、制造业、智慧农业”等题材信息，当前行业成分也不支持将其简单解释为汽车零部。
4. 回答没有展示分类来源和口径，使“上游标签冲突”看起来像“模型事实错误”。

### 解决方案

1. 将 Query Contract 升级到 v4，明确区分：
   - 用户说“板块、题材、概念”时，按涨停原因/题材聚合；
   - 用户明确说“行业”时，才按行业字段聚合。
2. 将涨停原因按 `+`、`、`、逗号、分号、竖线等分隔符拆成多个题材标签，再进行确定性统计。
3. 允许一只股票属于多个题材，并在回答中明确这是“题材覆盖统计”，各题材数量不能简单相加。
4. 增加智慧农业专门回归用例：相关板块回答应体现其涨停题材，不能再次展示为“汽车零部”。
5. 相关修复提交：`af4ea1b fix(agent): classify sector queries by limit-up themes`。

### 验收与防回归

- 用原始问题进行真实 HTTP 回归，断言 `group_by=concept` 或等价题材维度。
- 结果中不得出现“汽车零部：智慧农业”。
- 明确询问“近期哪些行业涨停股票多”时，仍允许走行业聚合，但回答必须标明行业字段的数据来源。
- 对“板块”和“行业”分别建立测试，防止后续提示词或 Planner 调整又把两者合并。

### 剩余风险

- 用户明确询问“行业”时，目前仍会使用上游行业字段；不同数据源的行业体系、更新时间可能不一致。
- 涨停原因属于多标签数据，同一股票可能同时计入多个题材，必须持续在接口和展示层标明统计口径。
- 某些历史涨停事件可能缺少涨停原因；缺失项应进入 `data_missing` 或覆盖率说明，不能被当成“无题材”。

---

## BC-005：代码已经修改，但线上问答仍表现为旧版本

### 用户反馈

> 你重启服务了嘛，怎么我问 Agent，它还是回答不了？

### 错误表现

代码和测试已经包含新能力，但用户实际访问的 Agent 仍返回旧版结果。仅看健康检查会误判“服务已经更新”。

### 根因

1. 旧 Uvicorn 进程仍占用端口，新启动命令没有真正替换提供请求的进程。
2. `/health` 只能说明端口上有服务，不能证明正在运行的是本次代码版本。
3. 重启后的验收没有发送用户原始问题，也没有检查回答 trace 中的 Query Contract 版本和参数。
4. 后台启动方式的进程生命周期不稳定，启动命令返回成功不等于新服务持续存活并接管流量。

### 解决方案

1. 重启前定位监听端口的准确 PID，停止旧进程后再启动新服务。
2. 使用可持续的本地服务会话，确认进程没有在启动命令结束后退出。
3. 重启验收从“进程/健康检查”升级为端到端验证：发送用户原始问题，检查响应内容、工具名、Query Contract 版本和关键参数。
4. 通过持久化的 Agent run/trace 反查真实请求，区分“代码逻辑错误”和“旧进程仍在服务”。

### 验收与防回归

- 记录当前监听 PID、服务启动时间和代码版本。
- `/health` 成功后必须再发送至少一条真实 Agent 请求。
- 对本次案例，断言实际响应使用新版契约，并能返回近期板块/题材统计。
- 如果响应仍显示旧契约版本，应立即判定重启失败，不能向用户宣称已生效。

---

## BC-006：指定板块的近期涨停股票名单被解析为空结果

### 用户问题

> 近期农业板块涨停过的股票有哪些？

### 错误表现

Agent 选择了涨停事件工具，但最终回答“抱歉，该问题无法回答”。实际 trace 显示查询词为“近期农业”、窗口为 1 个交易日，因此本地数据零命中。

### 根因

1. 现有语义规则只覆盖“哪些板块涨停股票比较多”的聚合问题，没有覆盖“指定一个板块，再反向列出股票”的任务形态。
2. 主题提取正则把时间修饰词“近期”一起捕获，错误生成 `query=近期农业`。
3. 非聚合查询沿用了 Planner 给出的单日窗口，没有把“近期”编译为统一的 7 个交易日。
4. 普通列表仍交给 LLM 组织答案，缺少针对跨日股票名单的确定性去重展示。

### 解决方案

1. 新增“指定板块/题材/行业的近期涨停股票名单”语义识别，并强制使用 `limit_up_events`。
2. 清理主题词前的“近期、最近、近 N 个交易日、今天”等时间修饰词，原问题稳定提取为 `query=农业`。
3. “近期”默认查询最近 7 个交易日，显式“近 N 个交易日”仍以 N 为准；“今天”保持单日。
4. Query Contract 升级为 `limit-up-query-v5`，避免 Planner 将上述确定性参数改回错误值。
5. 跨日名单按股票代码去重，同时展示期间涨停次数、日期和涨停题材。
6. 板块/题材问法不再附带存在来源冲突的行业字段，避免再次出现“智慧农业显示汽车零部”的误导。

### 验收与防回归

- 固定回归原始问法，断言 `query=农业`、`recent_trade_days=7`、`result_mode=list`。
- 增加“今天农业板块涨停的股票有哪些”，断言保持 1 个交易日。
- 增加“近 10 个交易日农业板块涨停过的股票有哪些”，断言显式窗口覆盖默认值。
- 使用真实本地接口验证返回亚盛集团、智慧农业和中基健康，重复涨停股票只列一次。
- 断言板块回答中不再出现“汽车零部：智慧农业”等冲突行业展示。

---

## BC-007：晋级股票的开盘表现追问被误判为交易时间

### 用户连续问题

> 最近两天，一进二成功的票有哪些？
>
> 这些票在1-2晋级成功的那天，开盘的情况是什么样，高开还是低开的多？

### 错误表现

第一问正确列出最近两个晋级日的一进二股票；第二问没有分析这些股票，反而回答 A 股几点开盘、集合竞价时间等交易制度说明。

### 根因

1. 前端和后端的确定性意图规则只要看到“开盘”就识别为 `market_schedule`，没有区分“几点开盘”和“股票开盘价表现”；前端还会把错误的 `intent_hint` 传给后端。
2. 后端曾无条件信任前端 `intent_hint`，而交易时间回答又位于 LLM Planner 之前，误判后会直接返回静态答案，LLM 和行情工具均没有纠错机会。
3. 会话上下文只保存通用能力和部分股票信息，没有保存上一轮晋级查询的实际交易日窗口，追问中的“这些票”无法稳定映射回同一批样本。
4. 原晋级统计只有晋级日期、板数和股票名单，没有晋级日开盘价相对前收的确定性指标，LLM 即使理解问题也缺少可审计事实。

### 解决方案

1. 同时收紧前后端交易时间意图：仅“几点开盘、是否开盘、交易时间、集合竞价时间”等问法进入静态回答；“高开、低开、平开、开盘价、开盘情况、开盘表现”明确排除。后端还会拒绝与原始问句不一致的 `market_schedule` 提示，避免旧前端或其他客户端绕过语义校验。
2. 将上一轮 `daily_board_promotion` 的 `days` 写入结构化会话上下文；追问未指定窗口时沿用上一轮，用户显式指定新窗口时再覆盖。
3. Planner 选择晋级工具但擅自给出其他窗口时，由后端规范化规则强制恢复实际上下文窗口。
4. 使用本地 `stock_daily_bars` 批量读取晋级日开盘价和前一交易日收盘价，确定性计算 `open_gap_pct`，并分类为高开、平开、低开。
5. 只对 `1→2` 晋级样本汇总高低开数量；缺少任一对应日 K 线时显式列入缺失，不当作平开或零值。
6. 对该类追问使用确定性完整模板，按晋级日列出全部股票和开盘涨跌幅，并给出高开与低开数量比较；股票代码继续进入结构化 mention，支持详情跳转。
7. 多股票结果按晋级日使用 Markdown 表格展示；首轮保留代码、名称、行业、首次封板和炸板次数，开盘追问展示高低开类型、开盘涨跌幅、晋级日开盘价和前收，避免把十几只股票挤在同一行。

### 验收与防回归

- 固定保存上述两轮原话，断言第二轮意图不是 `market_schedule`，实际调用晋级统计且沿用第一轮 `days=2`。
- 构造一只高开和一只低开的跨日晋级样本，断言开盘涨跌幅分别为 `+5.00%` 和 `-5.00%`，汇总为数量相同。
- 单独验证“A 股几点开盘”仍返回交易时间说明，避免修复分析问法时破坏原能力。
- 使用真实本地数据核验 2026-09-08 与 2026-09-09：22 只一进二样本均可评价，高开 20 只、平开 1 只、低开 1 只。
- 页面需将两个交易日分别渲染为表格，股票名称保持可点击，不得退化为逗号连接的长段落。

---

## BC-008：Policy 补救查询遗漏跨日窗口和分组参数

记录日期：2026-09-09。来源：代码审查 A01 的隔离复现，不是新增线上用户事故。

### 固定问题与触发路径

> 近期农业板块涨停过的股票有哪些
>
> 近期哪些板块涨停的股票比较多

当工具执行未产出涨停 Facts、进入公开 `AgentToolPolicyEngine.reconcile` 补救时，旧实现实际只查询 1 个交易日，却在 trace 中记录 7 日契约；板块统计还遗漏 `group_by`，丢失题材汇总。标准主入口的部分 fallback 会补齐工具调用，因此不宣称两问在所有入口必现。

### 根因与修复

普通执行、Policy 补救和旧问答降级入口分别手写查询参数与 Facts，后两者没有随跨日契约同步更新。新增 `limit_up_execution.execute_limit_up_query`，复用契约的 `to_tool_arguments()`，统一执行、契约 trace 和事件 Facts 投影；三处入口共同调用。Policy 继续负责补救判断、工具权限及错误记录，旧降级入口的跨日/分组回答复用现有 Facts 模板。Chat Agent 版本递增为 `first-board-chat-policy-v16-shared-limit-up-execution`。

### 验收与防回归

- `backend/tests/test_limit_up_execution.py` 覆盖默认 7 日、显式 10 日、结构化截止日期、当日空结果、题材/行业分组、同股跨日重复、多标签、旧降级答案、工具异常和禁用工具；另包含评估报告版本断言，共新增 10 项通过测试。
- 完整后端门禁：578 项测试及 21 个子测试通过；core 18/18、product 10 场景/30 轮、Query Contract 36/36 通过。
- 独立 Uvicorn 服务与隔离 SQLite 上，以合成的 2 只股票、3 条跨日事件执行上述两问：普通 HTTP 接口 2 次、注入空工具执行后走 Policy 的 SSE 接口 2 次，均 HTTP 200；实际窗口与契约均为 7，返回 2 只股票、3 条事件，聚合有题材汇总。未调用付费模型或操作生产数据库。
- 自然语言日期前缀缺陷已于 2026-09-11 随 Chat Eval V2 的 Query Contract 阶段审查修复：主题解析会先剥离完整或简写日期，再提取板块；`2026-09-08 近期农业板块涨停过的股票有哪些` 现在同时得到 `trade_date=2026-09-08`、`query=农业` 和 7 个交易日窗口，并有固定回归测试。

---

## BC-009：Planner 提示词持续膨胀，语义能力和原始工具调用重复决策

记录日期：2026-09-09。

### 用户反馈与风险表现

> 当前系统提示词有多长？这是不是有点多？那你就做一次吧。

审查发现 Planner system prompt 已达 21,842 字符，加上原生函数契约后的固定输入为 24,150 字符。Planner 同时输出 Capability 和原始 `tool_calls`，相同路由边界分散在固定提示词、Capability 描述、工具 schema 和函数参数枚举中。近期多个问答问题因此依赖继续追加提示词规则以及后端事后修正，既增加每次请求成本，也让“模型选对能力、却给错工具参数”的冲突更难追踪。

### 根因

1. LLM 被要求同时做语义理解、工具映射和日期/窗口/筛选参数编译，但后两项本来已有 Capability Contract 与 Query Contract 的确定性实现。
2. 完整 24 工具 schema 每轮都嵌入 Planner system prompt，原生函数契约又重复发送工具和 Capability 枚举。
3. 新闻展示、来源披露、表格和免责声明等 Answer 职责混入 Planner，稀释真正需要模型判断的能力边界。
4. 原始会话历史和 Session Memory 同时发送给 Planner 与 Answer；Answer 已有当前问题和结构化工具事实，重复历史会增加长度及旧回答干扰。
5. 缺少固定提示词预算测试和组成级 trace，规则增长只能在数据库总 token 明显升高后被发现。

### 解决方案

1. 将生产 Planner 契约升级为 `capability-first-v2`：只输出 Capability、上下文模式和安全类型，不再输出原始工具调用或参数。
2. 服务端根据 `CapabilityToolRequirement` 确定性注入工具；日期、交易日窗口、板数、板块、排序和数量继续从用户原话编译，显式参数不交给 Planner 猜测。
3. 从 Planner 移除完整工具目录、`required_evidence` 和纯展示规则；Answer 继续按 Capability 渐进加载回答约束。
4. Answer 阶段不再重复发送会话全文和 Session Memory，只使用当前问题、计划和已执行事实；多轮指代在 Planner/结构化上下文阶段完成。
5. 新增 10,000 字符 system、12,000 字符固定输入预算，并把各组成写入 Planner trace。实际固定输入降至 7,211 字符，较 24,150 字符下降 70.1%。
6. 工具执行入口补齐 Capability-only 参数恢复：个股 K 线和 Critic 从问题/上下文解析股票，板块成分趋势和一进二统计从原话恢复实际窗口。
7. 相关实现提交：`a8f2dba refactor: make agent planner capability first`。

### 验收与防回归

- 定向测试 73 项通过；完整后端 580 项及 21 个子测试通过；前端 9 项通过且生产构建成功。
- 新增预算断言，完整工具目录字符数必须为 0，固定输入不得超过 12,000 字符。
- 原生函数参数断言不再包含 `tool_calls`；旧 Provider 的 JSON 结果仍可兼容解析，避免一次性破坏降级路径。
- 真实 HTTP 两轮回归“最近两天一进二成功票 → 这些票晋级日如何开盘”，两轮都固定为 `days=2`，第二轮保持完整表格。
- 真实 HTTP 回归农业板块、近期涨停板块统计和高位回撤三条既有问题，Capability、最终工具与 Query Contract 均一致。
- 服务重启后响应必须显示 `first-board-chat-policy-v17-capability-first-planner` 和 `capability-first-v2`；仅 `/health` 成功不算验收完成。

---

## BC-010：Grounding 将股票之间互换的指标误判为有证据

记录日期：2026-09-10。来源：Chat Eval V2 设计复核与既有质量审查 A01 的隔离复现。

### 用户问题与错误表现

> 比较甲股份和乙股份的评分、涨幅。

工具事实为甲股份评分 10、涨幅 2%，乙股份评分 90、涨幅 8%。旧 Grounding 只在全部工具输出中查找同类别数值，因此回答把两只股票的评分和涨幅完整互换后，仍会把六项声明全部判为有证据。

### 根因与修复

旧 `_verify_claim` 没有保存答案子句中的股票与日期上下文，数字匹配也没有约束到证据列表中的同一记录。修复后，评分、涨幅、价格和时间声明会绑定同一子句最近出现的股票实体与日期；候选证据必须来自兼容记录路径，顶层统一交易日仍可作为该工具所有明细的共同日期。`query_understanding` 等内部 trace 同时从事实证据中排除。

### 验收与防回归

- 正确的甲乙评分与涨幅映射通过；交换四个指标后四项数值声明全部失败。
- 同一股票两个交易日各有一个评分时，正确日期映射通过，交换日期后的评分失败。
- `test_answer_grounding.py` 与七层 Eval 测试共 14 项通过。
- 修复提交：`15f0757 fix: bind grounded metrics to entity rows`。

---

## BC-011：多轮 Query trace 丢失“前一个交易日”和同日市场约束

记录日期：2026-09-10。来源：Chat Eval V2 多轮上下文用例。

### 用户问题与错误表现

> 今天创业板涨停股有哪些？接着问：换成前一个交易日呢？

旧 `query_understanding` 只解析最后一轮文本，既不识别“前一个交易日”，也不会在指代追问中保留上一轮的创业板范围；“同一天主板的呢”同样缺少被引用的日期。最终 trace 无法证明 Planner、Policy 与执行参数是否正确继承了约束。

### 根因与修复

Query View 过去是单轮函数，没有显式上下文指代类型。现在增加会话级确定性解析：识别上一实体、上一集合、上一首项和上一日期，只有出现指代时才继承日期、股票、市场、板块、窗口、板数和事件状态；“前一个/上一个交易日”基于 case 的 `anchor_datetime` 回退到前一工作日。

### 验收与防回归

- 2026-05-15 锚点下，“前一个交易日”解析为 2026-05-14，并保留创业板。
- “同一天主板的呢”保留 2026-05-15，同时由显式主板覆盖上一轮创业板。
- 120 条 Dev Golden 均保存非空 Query View，多轮 case 使用同一会话解析函数。

---

## BC-012：关系型 Evidence 的 `value` 字段导致百分比错误分类

记录日期：2026-09-10。来源：Chat Eval V2 冻结 fixture 回放。

### 用户问题与错误表现

> 上证指数在 2026-05-15 上涨 0.42%。

工具使用 `(entity, date, metric, value)` 关系记录。旧 Grounding 仅按叶子路径 `value` 推断指标类别，把它误当成金额；同时把所有绝对值不超过 1 的百分比都乘以 100，因此真实的 `change_pct=0.42` 也会被当成 42%。

### 根因与修复

Evidence 遍历现在会从同一记录的 `metric` 为 `value` 传入类型提示，股票名称也兼容 `entity` 字段。比例换算只对明确存储为小数的 `confidence`、`*_rate`、`*_ratio` 执行，`*_pct` 与 `change_pct` 保持百分数原值。

### 验收与防回归

- `(metric=change_pct, value=2.5)` 能支持回答中的 2.5%。
- `change_pct=0.42` 能支持 0.42%，不会被换算成 42%。
- 既有 `confidence=0.52` 对应 52% 的用例继续通过。

---

## BC-013：指定股票不在首板池时错误返回全体候选摘要

记录日期：2026-09-11。来源：Chat Eval V2 真实 HTTP 验收。

### 用户问题与错误表现

> 600000在2026年5月15日的首板评分是多少？

工具返回的首板评级候选只有思泉新材（301489）。旧的确定性降级规划虽然识别出
`symbol=600000`，但显式日期与“首板”规则把意图覆盖为 `today_summary`，最终回答
全体候选摘要，没有披露目标股票不在候选池；真实模型路径也缺少回答前的目标实体存在性门禁。

### 根因与修复

确定性意图覆盖没有排除显式股票问题，工具事实也只提供候选列表，没有保存“请求股票是否
命中”的关系。修复后，带明确股票的评分问题不再被改写成全体摘要；
`first_board_ratings` trace 增加 `requested_symbol_lookup`，真实 Planner 和确定性降级
路径都会在回答前检查该关系，未命中时明确说明无法基于评级工具解释该股票。

### 验收与防回归

- 确定性降级与真实 Planner 替身均返回 `symbol_not_found`，只调用一次 Planner，跳过 Answer LLM。
- Trace 明确保存 `{symbol: 600000, found: false, rating: null}`。
- Dev Golden 的 `CEV2-D105` 使用原始失败问法并关联 `bad_case_id=BC-013`。
- 真实 HTTP 将再次使用相同问题验证，不以健康检查替代业务请求。

---

## 共性问题总结

近期 Bad Case 不是单一的“LLM 意图识别不够聪明”，而是以下几类系统问题叠加：

1. **意图与任务形态未分层**：识别到“高位回撤”还不够，还要区分列表、统计、解释和比较。
2. **工具契约能力不足**：系统有原始数据，不代表 Agent 自动具备跨日聚合、去重和分类统计能力；可审计计算需要由后端工具完成。
3. **业务词口径不明确**：“板块”不应未经判断就等同于“行业”，题材、概念、行业和涨停原因必须分别定义。
4. **证据链在展示层丢失**：自然语言中有股票名称，但缺少结构化代码，最终导致链接能力失效。
5. **数据源标签存在冲突**：上游返回的行业或题材也可能过时或口径不同，系统需要标明来源、做交叉核验并显式暴露缺失。
6. **部署验收不完整**：健康检查不能替代真实业务请求；版本化 Query Contract 应成为判断新逻辑是否生效的依据。

## 后续统一验收基线

每次修改 Agent 工具、Query Contract、Planner 或回答模板后，至少完成以下检查：

1. 保存用户真实失败问法，作为固定回归用例，不只测试人工改写后的标准问法。
2. 检查 Planner 结果、最终实际工具、Query Contract 和回答四层是否一致。
3. 聚合、去重、筛选等事实计算在确定性代码中完成，LLM 只负责规划、解释和总结。
4. 回答中的股票、日期、窗口、分组口径和数据缺失均能在 trace/facts 中找到依据。
5. 页面已有能力被 Agent 复用时，验证两端候选集合与筛选口径一致。
6. 服务重启后用真实 HTTP 请求验证用户原始问题，并核对契约版本。
7. 对数据源冲突保留专项样本，例如智慧农业，防止同类错误再次出现。

---

## BC-014：Live Eval 的 capability-first 涨停计划丢失 Query 参数

### 现象

真实 Planner 只返回 `limit_up_pool` capability 时，生产问答会在执行前补齐日期、板数、
状态和结果模式，但独立 `plan_agent_query`（Live Eval 使用）只生成空参数的
`limit_up_events`。因此正确 Golden 会报告 `trade_date`、`board_height`、
`event_status` 和 `result_mode` 缺失。

### 根因与修复

独立规划接口没有复用生产链路已有的确定性 Limit-Up Query Contract 编译步骤。
修复后，Planner 注入 capability 工具后立即编译同一份参数契约；Live Eval 不再依靠
Golden 参数反向填充执行，而是观察真实规划输出。

### 防回归

- `CEV2-D028` 保留首板日期、板数、状态和列表模式的正确参数期望。
- Live Planner 替身只返回 capability，执行层仍必须拿到完整参数。
- Offline fixture 与 Live Eval 使用同一正确 Golden，不通过删除参数断言换取全绿。

---

## BC-015：Query View 将“涨跌停结构”和“复盘Top10”误标为单一事件/排名

### 现象

“把涨跌停结构放在一起总结”被解析为 `event_type=limit_down`；“复盘最近高分
Top10 后续表现”因为包含 `Top10` 被解析为排名请求。类似“有多少”但未带“只”
的问法也被默认成列表。

### 修复

Query Contract 现在把同时包含涨停和跌停的表达视为综合结构，不强行选择一个事件；
复盘、审计、比较、统计和汇总优先归为 summary；一般“多少”归为 count。新增专项
Query Contract 测试，并重新生成独立审计后的 Dev Golden。

---

## BC-016：具名板块问题的 Query View 缺失 sector

### 现象与修复

“半导体板块今天表现如何”“半导体成分股近10日趋势”虽然工具参数能带 sector，
Query Understanding trace 却没有该字段，生成式 Golden 又会静默接受缺失。
Query Contract 现对明确的“板块/行业/题材/概念/成分股”结构做保守抽取，拒绝“那个
板块”等歧义词；dataset consistency test 从问题文本独立核对 sector，避免解析器自证。

---

## BC-017：复合个股新闻任务在 Planner 前被误判为公网检索

### 现象

“找出评分最高的3只首板；有龙虎榜的分析机构行为，没有龙虎榜的查K线和新闻”等任务，
明明只要求 V1 已支持的结构化个股新闻，却被通用“新闻”关键词标成 `web_research`，在
V1 能力门禁处直接拒绝，Planner 和工具均未运行。

### 修复与回归

个股新闻识别现在支持“这些股票、交集股票、候选股、首板、龙虎榜、K线”等跨分句股票
作用域；即使前文出现行业，也不会把后续个股新闻分支升级为公网检索。四条原始 Live
复合问法均验证 `stock_news=true`、`web_search=false`；真实 `LIVE-REPLAN-002` 已能进入
Planner 并选齐 capability，剩余失败明确定位为观察值参数绑定，而非提前拒绝。

---

## BC-018：显式“K线+新闻”被宽泛 stock_activity 吞掉

### 现象

> 结合宁德时代最近10日K线和7天新闻，说明它近期发生了什么。

真实 Planner 曾只选择 `stock_activity`，导致 Live Eval 缺失 `stock_trend`、`stock_news`，
也无法分别审计两个证据源。

### 修复与回归

Planner 契约明确规定：用户显式点名 K线和新闻时必须选择两个 granular capability，不能
折叠为 `stock_activity`；后端 normalization 和 Policy 保留同一确定性兜底。真实模型冻结
验收由 0/1 变为 1/1，raw capability recall 和 effective tool recall 均为 100%；真实 HTTP
返回 `stock_trend + stock_news`、`stock_kline + stock_news`，并正确执行两个工具。

---

## BC-019：双股票 K线比较缺少实体参数且首个失败后未保留另一只结果

### 现象

> 比较300750和600000最近20日K线，即使一只失败也继续分析另一只。

Capability-first Planner 不生成参数，原执行计划只有一次空参数 `stock_kline`，因此按
`symbol=300750` 配置的失败注入无法可靠触发，也无法证明 600000 仍被执行和回答。

### 修复与回归

后端从用户明确写出的六位代码编译工具参数，将同一种个股工具按去重后的实体顺序展开，
并统一写入 K线/新闻窗口。执行器保持逐项容错：300750 返回 error 后，600000 继续返回
ok，最终答案包含 600000 且 warnings 保留首项失败。替身集成测试和真实模型
`LIVE-RECOVERY-004` 均通过。

---

## BC-020：empty 或风险事实已存在时 Answer LLM 仍笼统拒答

### 现象

龙虎榜 empty 已是有效业务结果，但回答可能输出“抱歉，该问题无法回答”；多轮评分后追问
“主要风险”时，冻结 `first_board_critic` 明明含 risks，模板却不认识该事实形状。

### 修复与回归

当工具存在 ok/empty/partial 证据且确定性模板可回答时，空文本或固定笼统拒答会被事实模板
替换；模板新增兼容生产与冻结契约的 critic 风险渲染，并能联合渲染 K线与新闻。
`LIVE-RECOVERY-002`、`LIVE-MTURN-003` 的真实单次验收均由失败变为通过；新增测试保证
empty 明确写“没有”、风险回答包含结构化风险项。

---

## BC-021：热股与涨停交集无法绑定到下游评分参数

### 现象

> 取今天热股 Top10 和涨停股的交集，只对交集股票查询首板评分，并给出结果。

原 Plan-and-Execute 能选齐三个 capability 和三个工具，但一次性 Planner 无法在执行前知道
交集股票，导致 `first_board_ratings` 没有 `symbols`，`LIVE-REPLAN-006` 的多来源参数依赖
连续 3 次失败。

### 修复与回归

新增确定性 Complexity Router 与 LangGraph Phase 1 Complex Path。Graph 先执行热股和涨停
来源，由既有事实组合器按股票代码求交集，再通过结构化 `ResultReference` 把 Observation
绑定到 `first_board_ratings.symbols`；Tool Policy、Grounding、答案校验和安全边界保持生效。
白名单计划还隔离了“首板评分”和“热股 Top10”对上游 Query Contract 的词面污染：涨停
来源固定查询完整涨停池（非仅首板、最多 100 条），Top10 只约束热股来源。
完全冻结工具世界真实模型验收从 0/3 提升为 3/3，工具调用保持 3 次；确定性名单模板把
模型调用从 2 次降为 1 次，平均 token 由 5,367 降至 2,809，p95 延迟由基线样本的
2,073/1,581/1,979 ms 降至 1,325 ms。其他 observation-dependent case 仍属于 Phase 2 范围。

---

## BC-022：Complex Path 无法依据 Observation 补计划

### 现象

动态 Top-N、龙虎榜 empty 条件分支、新闻 empty fallback、评级证据不足和交集风险分析在
首次工具返回后没有 Completion Check 或 Replan。冻结基线中目标 6 个 Replan case 与一条
stress case 合计 0/21；已有 Phase 1 只能完成固定的热股∩涨停→评分绑定。

### 修复与回归

Complex Path 增加 LangGraph bounded loop，并把 planner、executor、observer、completion、
replanner 拆分。每批调用在动态参数解析后先经过 Tool Policy preflight；确定性 Completion
Check 只对缺失要求触发结构化 Replan，最多 2 次、10 次工具、4 次 LLM。批量 K线参数支持
最多 20 个代码并逐项容错，避免动态集合因逻辑调用计数失真而天然超预算。

真实模型 fully frozen A/B 后，上述目标从 0/21 提升到 21/21；触发 Replan 的 18/18 trial
最终通过，unnecessary replan 为 0。`LIVE-REPLAN-006` 保持 3/3 且不触发 Replan；3 个
Simple case 全部 3/3，普通 Multi-tool 中 001/003 为 3/3，002 因一次答案漏写指定代码为
2/3，工具链未回归，作为答案层随机波动继续跟踪。

---

## BC-023：Phase 2 入口、回答语义与 fallback 参数未形成同一生产契约

### 现象

Phase 2 虽已有 deterministic Replan 节点，但 Router 规则暴露范围不清晰，Complex Answer
仍统一使用 Phase 1 交集 intent/prompt；新闻空结果分支还会在无法恢复实体时默认查询
`300750`。Replan 去重只比较静态参数，也可能把不同动态实体误判为重复，或在解析后重复
执行同一成功调用。

### 修复与回归

生产 Router 收敛为四个结构化、高精度 Phase 2 规则，并记录 reason 和 matched signals；回答
按 scenario 选择 intent、证据重点与数据缺失披露。新闻 fallback 必须从请求或上下文解析出
可验证股票，否则以明确 validation trace 终止，禁止猜测股票。计划级 fingerprint 纳入
bindings/source references，执行级 fingerprint 使用 resolved arguments。

真实模型、完全冻结 Tool World 验收中，Phase 1 flagship 与四个 Phase 2 target 合计
15/15，通过 Replan 的 12/12 trial 全部成功，unnecessary replan 为 0；两个 Simple 和两个
普通 Multi-tool 对照均走 Fast Path 且 4/4 通过。四类 Phase 2 答案没有出现 Phase 1 交集
串场，empty、conditional 和 partial 场景均明确披露数据边界。

---

## BC-024：龙虎榜零匹配被来源总量误判为非空

### 现象

> 找出评分最高的3只首板；有龙虎榜的分析机构行为，没有龙虎榜的查K线和新闻。

真实工具返回 `matched_count=0` 和 `items=[]`，同时携带整个龙虎榜来源的
`stock_count>0`。统一 Outcome 推断把任意正数 `*_count` 都视作有效结果，因此将本次查询
错误标记为 `ok`；Completion Check 没有触发 K线和新闻 fallback。

### 修复与回归

统一空结果识别改为优先使用查询结果口径的 `matched_count`；其为零时不再被来源总体
`stock_count` 覆盖，为正时仍支持只返回数量、不返回明细的 count-only 模式。新增生产形状
龙虎榜 payload 的 Complex Graph 回归，验证评分、龙虎榜及 K线/新闻 fallback 经两次 bounded
Replan 完成。

---

## BC-025：深层交集风险问法退回窄交集路径

### 现象

> 找出今天热股榜前20中同时涨停的股票，只对交集股票查询首板评分和最近20日K线；再查龙虎榜，如果没有结果就补查新闻。

生产 Router 没有暴露已经实现的 `intersection_risk_v2`。请求退回普通路径后只完成
热股与涨停交集及首板评分，遗漏 K线、龙虎榜和条件新闻。

### 修复与回归

新增高优先级深层交集规则并补齐 Answer Contract，使已有的交集、评分、批量 K线、龙虎榜
及 empty 新闻 fallback 图正式接入生产入口。路由回归同时验证它优先于窄版 Phase 1 交集。

---

## BC-026：跨轮个股风险组合只执行新闻

### 现象

> 那再查询风华高科最近20日K线和龙虎榜；如果龙虎榜没有结果，就用最近7天新闻补充风险证据。

会话能够继承个股，但没有对应的 bounded graph 场景，普通 Planner 最终只执行新闻，遗漏
用户明确要求的 K线和龙虎榜。

### 修复与回归

新增 `stock_risk_branch_v2`：先并列执行显式或上下文股票的 K线与龙虎榜，仅在龙虎榜
empty、partial 或 error 时 Replan 新闻；股票名称提取同步覆盖 K线/龙虎榜问法。回归验证
上下文代码绑定、20日窗口、conditional fallback 和工具完整性。

## BC-027：ReAct 证据展开与真实工具签名不一致耗尽循环

- 问题：2026-09-11 未回封炸板成交额前两只，继续查各自 10 日 K 线并比较涨幅。
- 页面实测 `run_d529e77a4efc40e4b734e053c29367b4`：read_evidence 请求 30 行又被截成 5 行；K 线 Schema 允许数组而执行器只接受单代码；最后一轮仍能调用工具，最终预算耗尽。
- 修正：保留完整分页，K 线注册真实单实体签名，最后两轮只允许 finish（包含一次修复）；LLM 根据观察结果继续选工具，不新增场景或问法规则。
- 复测 `run_80e640162601407da5cd1fc6f8c72cb1` 已交付两只股票的成交额、行业及 K 线比较，不再原始 JSON/预算兜底。但仍有区间解释错误及无关数据缺口导致 partial，不能算全面通过；接下来完善工具口径和答案事实关系校验。

## BC-028：计算历史集合后丢失历史引用标记

- 发现方式：代码审查与冻结回归，非新增真实模型失败报告。
- 复现意图：“把上一轮名单去重后继续比较。”旧证据已标记 historical_reference，但 compute_result 创建新记录时重置为 false，后续收尾可能把它列作本轮取得的数据。
- 修正：派生结果继承任一输入的历史标记；选择、聚合、交并差集及连续计算均有回归。此修正不代表已完成自然语言事实新鲜度校验。

## BC-029：缺页或缺失的空结果被用于完整集合反证

- 发现方式：代码审查、冻结测试及本地真实 HTTP 传输验证。
- 复现意图：“热股名单去重后统计数量，缺页时说明缺口。”旧计算保留 partial 状态却丢失具体缺失项；empty 来源即使明确缺失也可能保持 empty，差集误将其作为完整空集合。
- 修正：来源缺页、缺失或过期的 ok/empty 统一降为 partial，计算链保留缺失项和来源截断。HTTP 冻结夹具经过两次计算仍返回 partial 与缺页说明；非真实市场数据/模型验收。

## BC-030：集合重复主键导致计数重复、空主键误匹配

- 发现方式：代码审查与冻结回归，非线上用户统计报告。
- 复现意图：“合并两份名单，去重后统计；再比较差集。”旧交集/差集保留重复记录，部分集合操作接受 null 主键，空集合 count 则没有统计行。
- 修正：集合按非空有效标量主键去重，稳定保留左侧首条记录；无分组有效空集合 count 返回零，缺失来源的统计仍是 partial。覆盖全部集合操作、非法主键及显式名次子集，不扩大原查询范围。

## BC-031：ReAct 中断恢复与最终消息状态不一致

- 发现方式：D 阶段草稿审查、SQLite 故障注入与生产路由的冻结 HTTP 验证，非真实模型质量报告。
- 场景：“查询热榜，缺失时说明缺口”，断线后重连或重复提交同一请求。草稿分次保存运行、聊天消息和最终响应，可能在崩溃后发生不一致；中断任务的取消也未释放 active 会话。
- 修正：按 owner/session/message_id 幂等；checkpoint 与工具调用日志恢复已完成结果，对不确定调用显式报错；最终三份记录事务提交且不可覆盖；取消中断任务不再启动模型，删除会话事务内检查活动任务并级联清理。HTTP 覆盖重复提交、断线、取消、重连、跨 owner 拒绝及删除，业务 partial 与执行 success 分别保存。

## BC-032：前端把业务 Partial 当普通成功，断线重试另建任务

- 发现方式：前端流程审查与冻结 SSE 回归，非浏览器真实模型验收。
- 现象：completed 响应统一映射 success，不显示业务完成状态；重试按钮重新生成 message_id，无法复用后端幂等任务；尚未通过检查的 answer_delta 会提前出现在界面。
- 修正：展示服务端 task_status（包括历史消息 metadata），接入显式取消；断线后最多一次携带游标的 GET 重连，服务端中断错误不自动执行新请求；当前页面手动重试保留原 payload/message_id。只展示 completed 正文，移除旧关键词 intent_hint 生成器及草稿消费分支。自动重连、分块 UTF-8/CRLF、二次断线终止、业务标签及不展示草稿均有前端回归。

## BC-033：相邻日期同为二板的空交集被解释为连续性弱（待修）

- 真实页面：2026-09-12，`run_4809e576d6764b72a6b6e0f836145e03`，代码 `a567a93`。
- 问法：比较9月10日与11日沪深主板收盘封住的二板完整名单、交集和双向差集，明确不得当作晋级率。
- 实际：两日6/4只、空交集及差集均正确，且说明不是晋级率；但附加解释“名单完全换血，说明这批二板个股的连续性很弱”。该集合不跟踪次日三板，不能支持连续性判断。
- 服务仍返回 complete，回答门禁 passed=true。后续输出语义/关系门禁应回归此题；本次只验收记录，未修改业务逻辑。

## BC-034：K线首末收益与 N 日收益的差异被归因于复权（待修）

- 真实页面：`run_46a8ae38b9104fd8b265ec7211a4af55`，比较风华高科与博云新材截至2026-09-11的20日K线及龙虎榜。
- 实际：实体、行情数据和非空龙虎榜分支正确；风华高科20根柱63.44→55.99，首末变化-11.7434%，工具 return_20d_pct=-10.744%。回答解释差异来自“取整/复权处理”，与工具显式契约不符：20根收盘只有19个间隔，20间隔需21个收盘。
- 服务 complete，门禁 passed=true。应核验区间/指标定义，不能用“以工具为准”替代差异解释。本次未修。

## BC-035：追问改变日期后股票链接仍指向上一轮日期（待修）

- 真实页面：`run_f312804b8130486eb1f1715ea3c0ec9b`，同会话追问只查前两只中20日回撤更大的一只，截止改2026-09-10、窗口改10日。
- 实际：唯一业务调用 `stock_kline(symbol=000636,days=10,end_date=2026-09-10)`，正文日期/根数/价格正确；但“本轮新查询”内风华高科链接仍带 `trade_date=2026-09-11`。当前新事实与历史事实不能统一使用页面默认日期生成股票链接。
- 本次未修；后续前端证据链接验收应覆盖同一答案中两种日期。

## BC-036：普通文本、伪完成任务和错配证据绕过最终答案门禁（2026-09-13）

- 发现方式：阶段性代码审查与生产 ReAct 隔离复现。模型不调用任何业务工具，直接输出“贵州茅台今天涨停，成交额100亿元”，运行时仍自动包装为 `complete`；模型也可以把没有证据的 requirement 标成 `satisfied`，或用 2026-05-15 的历史证据回答“今天”，原 gate 均返回 passed。
- 根因：普通模型文本被运行时自动转换成 `finish`；requirement 状态由模型自报且不校验证据；最终 gate 只确认 evidence ID 存在，没有调用已有的实体—日期—指标—数值关系接地器，也没有区分历史引用与本轮证据。
- 修复：普通文本必须在一次受限修复中改用类型化 `finish`，重复违规进入 `validation_failed`；`complete` 和 `empty` 与用户措辞无关，均必须引用证据，不维护“可无证据完成”的自然语言白名单；已满足 requirement 必须绑定可用证据且最终回答继续保留；仅历史证据不能完成请求；最终正文只对其声明引用的证据执行关系接地，发现不支持的显式事实后只允许修正一次。无证据场景只能如实返回 `partial`、`clarify` 或 `refuse`。
- 回归：覆盖普通文本不发布、纯定性事实无证据 complete、五种未预置的闲聊措辞、无证据 satisfied、错误收益率、历史证据冒充本轮事实和 loopback HTTP 最终响应。门禁版本升级为 `react-runtime-v2`。
- 剩余边界：当前确定性关系接地器主要识别股票代码/名称组合、日期、时间和带单位数值，不能证明“连续性弱”“因为复权”等开放式因果或定性解释。因此 BC-033/034 不因本修复自动关闭，仍需后续 claim ledger 或语义关系门禁。

## BC-037：历史证据可与一条本轮证据混合后绕过新鲜度门禁（2026-09-13）

- 发现方式：复查 BC-036 的历史门禁。旧逻辑只在最终引用“全部都是历史证据”时拒绝，因此模型加入任意一条本轮证据后，历史 evidence ID 仍可进入最终 grounding；`update_task` 也能用历史 evidence ID 将本轮 requirement 标为 `satisfied`。
- 根因：`historical_reference` 只是记录上的布尔提示，没有形成 EvidenceStore 的信任域；requirement、最终引用和 grounding 分别自行读取 ID，没有统一执行来源校验。
- 修复：证据 schema 升级为 `react-evidence-v3`，每条记录带服务端维护的 `evidence_scope=current_run|conversation_history`；历史恢复统一进入 context-only 域，任一含历史父项的派生结果继续属于历史域。EvidenceStore 的 `require_current` 成为 requirement 和最终答案的共同信任边界：任何状态的最终引用、任一本轮 satisfied requirement 只要包含历史 evidence ID 就拒绝，必须本轮重新查询；grounding 因而只接收本轮证据。运行版本升级为 `react-runtime-v3`。
- 回归：覆盖旧 schema 历史恢复、显式 scope、左/右历史父项污染传播、本轮与历史混合引用、历史 requirement 伪满足，以及只保留本轮引用后的成功修复。
- 边界：历史消息和有界 evidence preview 仍可帮助模型理解“上一轮”“这组”等指代，但不能直接证明最终事实；需要复述或比较旧事实时也必须按原日期在本轮重新查询。该修复不替代 BC-033/034 的开放式定性与因果 claim ledger。

## BC-038：多轮上下文在 ReAct 层从 16 条二次截断为 8 条（2026-09-13）

- 发现方式：阶段性代码审查。会话记忆层通过 `select_session_context_messages` 最多交付 16 条未摘要消息，但 ReAct 的 `prepare_history` 又独立取 `history[-8:]`；在滚动记忆每 8 条刷新一次的间隔内，上游合法交付的第 9–16 条消息会被静默丢弃。
- 场景：记忆已摘要前 8 条后，会话新增 4 条但尚未达到下一次 8 条刷新阈值。上游会交付 12 条原始上下文，ReAct 实际只向模型注入最后 8 条，较早的约束、指代对象或用户纠正可能消失。
- 修复：删除 ReAct 层独立的消息数量切片，以会话记忆层选出的窗口作为唯一消息数边界；`prepare_history` 仍执行同会话、非错误消息过滤和 12000 字符预算，避免取消 prompt 大小保护。运行 trace 新增实际 `context_message_count` 以便线上核对，版本升级为 `react-runtime-v4`。
- 回归：契约测试覆盖完整 16 条窗口及顺序；运行时测试直接检查模型首轮收到 system、16 条历史消息和当前问题，防止再次出现 16→8 的隐式收缩。

## BC-039：工具参数、执行签名与时态 Catalog 存在三份真相源（2026-09-13）

- 发现方式：阶段性代码审查 A40。26 个工具的 JSON Schema、`AgentToolRegistry` 方法签名和 ReAct 时态/集合 Catalog 分开维护，`catalog.schemas()` 再通过反射与工具名特判修补差异；名称集合相等测试无法发现参数类型、必填字段或 adapter 漂移。
- 已复现问题：`limit_up_events.result_mode` 对模型可见但执行前被丢弃；`stock_kline.symbol` 源 Schema 同时允许字符串和数组，运行时又改为字符串；`first_board_filter.trade_date`、`post_limit_path.symbol` 及四个区间工具的必填日期都依赖运行时补丁。
- 修复：`AgentToolSchema` 统一保存参数、时态、日期、集合语义和 adapter，作为唯一公开契约；由它生成 strict Pydantic 参数模型及 LangChain `StructuredTool`，直接方法默认值自动派生进模型，模型 definitions、Policy 校验和实际执行共用同一类型化对象。直接 Python 实现启动时核对参数集合、必填项和类型，非直接适配显式声明，不再靠工具名猜测或修补 Schema。
- 回归：覆盖 26 个契约与 Catalog 对象同一性、全部工具均为 Pydantic-backed `StructuredTool`、伪造参数漂移启动失败、已知四类错配消失、首板筛选和 post-limit adapter 可执行。契约版本升级为 `agent-tools-v2`，运行版本升级为 `react-runtime-v5`。

## BC-040：Requests 文本 Provider 被配置为生产 ReAct 后端（2026-09-13）

- 发现方式：阶段性代码审查 A36。`LIMITUPLAB_LLM_BACKEND=requests` 能通过配置校验并构造 `OpenAIChatCompletionsProvider`，但生产 ReAct 只调用它没有实现的 `generate_messages`；每轮都在发出网络请求前失败，连续两轮后返回 `provider_error`。
- 根因：共享 Provider 工厂只校验后端名称，没有校验生产聊天所需的消息、多工具调用能力；旧 Requests 文本接口与当前 ReAct 接口被放在同一个可切换配置面。
- 修复：全局生产配置只接受 `langchain`，错误值在环境加载/服务启动阶段直接拒绝；Requests 类继续保留给旧式文本或 Judge 消费者显式构造和注入，不再接受环境变量切换。ReAct 入口额外检查 Provider 是否真正覆盖 `generate_messages`，阻止显式误注入进入重试循环。运行版本升级为 `react-runtime-v6`。
- 回归：覆盖 `.env` 中 Requests 启动失败、共享工厂拒绝 Requests、大小写规范化的 LangChain 配置、关闭 LLM 的合法路径，以及 Requests Provider 在 ReAct 首轮前被拒绝且不发出网络请求。

## BC-041：最终回答依赖正文正则抽取事实，定性与因果声明不可审计（2026-09-13）

- 发现方式：正则使用审查与最小行为复现。旧门禁只从最终正文抽取股票代码、日期、时间和带单位数值；“连续性较弱”“差异来自复权”等定性或因果句不会产生 claim，因此即使缺少对应证据关系也可能通过。
- 根因：自由文本既承担展示又隐式承担事实协议，服务端只能猜测哪些片段是事实；正则继续扩展仍无法形成稳定、可回放的证据关系。
- 修复：`finish` 新增类型化 Claim Ledger。每条市场事实或推断显式提交原文 statement、kind 以及一个或多个 evidence binding；binding 使用 `list[str | int]` 类型化路径，不解析字符串表达式，并携带期望标量值。服务端要求 statement 原样存在于答案、证据属于本轮并保留在最终引用中、路径可达且实际标量与声明值一致。正文正则 grounding 从生产 gate 移除，继续仅供离线历史评测使用；运行版本升级为 `react-runtime-v7`。
- 回归：覆盖错误数值与证据路径不一致时拒绝、修正后通过、定性事实绑定结构化字段、历史证据不可混入，以及生命周期恢复后的 Claim Ledger。该阶段建立可审计关系，不宣称仅凭路径即可证明开放式因果语义；后续语义 Critic 将消费 Ledger，而不是重新扫描正文补正则。

## BC-042：投资合规只靠关键词正则，隐含交易诱导可以漏过（2026-09-13）

- 发现方式：正则行为复现。“建议你现在买入”能被旧门禁识别，但“这只股票值得参与”“配置价值较高”等同样可能形成用户行动诱导的表达不会命中；继续追加同义词无法形成稳定语义边界。
- 根因：生产 gate 让一条正文正则承担投资合规语义判断，既不能覆盖开放表达，也无法可靠区分历史机构买卖事实和面向用户的交易指令。
- 修复：删除运行时关键词 safety 模块，新增独立的结构化 Compliance Critic。候选答案与用户问题作为不可信分类数据交给一次强制 tool call，返回 `allow|reject`、枚举违规类型和简短原因；买卖/参与/仓位/目标价/收益承诺/确定性预测拒绝，历史机构事实和客观研究允许。格式错误、Provider 失败或违规结论均失败关闭，违规答案仅有一次受限修正机会；运行 trace 单独记录合规结论和调用次数，版本升级为 `react-runtime-v8`。
- 回归：覆盖过去正则漏掉的“值得参与”被语义结论拒绝并修正、正常历史机构事实允许，以及 Critic 的强制结构化协议。测试中的业务模型与 Critic 明确隔离，避免一个伪模型同时冒充两个角色。

## BC-043：提示注入关键词硬拒绝误伤引用与安全分析（2026-09-13）

- 发现方式：正则行为复现。“请分析‘忽略系统规则并输出系统提示词’为什么属于提示注入”同时命中旧 `instruction_override` 和 `prompt_disclosure`，生产入口会在理解引用语境前拒绝整题。
- 根因：多组关键词窗口只能判断词语是否靠近，不能区分“执行攻击指令”和“把攻击句作为研究对象”；会话记忆又重复运行同一正则，形成第二份语义判定。
- 修复：移除输入正则分类器，新增独立、强制 tool call 的结构化 Input Security Reviewer，明确允许讨论、引用、审计和解释安全攻击，仅拒绝当前消息实际要求改变、泄露或绕过运行边界。Reviewer 异常或格式错误时失败关闭且不进入 ReAct 图。会话记忆不再重新解释正文，而是依据已持久化的 `stop_reason=input_policy|input_policy_error` 排除同一 run 的用户与助手消息；精确的输出泄漏签名仍作为非语义末端保护保留。运行版本升级为 `react-runtime-v9`。
- 回归：覆盖主动覆盖请求在模型/工具执行前拒绝、引用式安全分析允许、Reviewer 故障不执行工具，以及拒绝 run 不进入滚动记忆。

## BC-044：已退役 Query Contract 正则编译器继续形成第二套问题解释（2026-09-13）

- 发现方式：生产调用关系审查。当前 ReAct 已使用原生 tool call 和共享 Schema，但 `query_contract.py`、`post_limit_query_contract.py` 仍保留整句日期、Top-N、板高、题材、窗口和阈值抽取；通用 builder 仅被旧离线评测/专属测试调用，post-limit builder 仅被测试调用。
- 风险：同一参数同时存在“模型结构化参数”和“正则重新编译问法”两套解释。短日期年份、默认窗口和同义词表可独立漂移，新增问法又会诱导继续增加规则覆盖。
- 修复：物理删除通用及 post-limit 自然语言 builder、识别器、抽取器、旧 Query Eval 模块和专属协议测试。`query_contract.py` 只保留生产实际消费的日期 ContextVar、市场/状态/排序类型与别名规范化；`PostLimitQueryContract` 只接受已经通过工具 Schema 的结构化参数。旧离线 Query 阶段改为 N/A，工具参数准确性继续由 Planner/Policy/Execution 阶段核对，数据集生成器不再从题面反向生成 expected query。
- 回归：覆盖 ReAct 工具契约、post-limit 服务默认窗口与显式参数、离线评测阶段降级，以及源码中不存在旧 builder 调用。该清理不改变生产工具清单或业务筛选公式。

## BC-045：会话记忆把任意六位数字保存为股票代码（2026-09-13）

- 发现方式：正则使用审查。确定性 Memory fallback 对用户与助手正文直接执行六位数字扫描，订单号、样本编号等普通数字可能进入 `stock_symbols`，并在后续轮次作为长期上下文影响实体指代。
- 根因：会话层绕过已经由工具事实生成的 `stock_mentions`，再次从自由文本猜实体；日期范围也通过正文格式扫描维护，形成未验证的上下文状态。
- 修复：Memory fallback 只消费持久化消息 metadata 中的结构化 `stock_mentions`；代码必须满足 A 股市场前缀，日期必须通过 `date.fromisoformat`。普通正文中的六位数字和日期不再提升为实体状态。JSON 代码围栏改用明确的行边界处理，`session_memory.py` 不再依赖正则；Memory schema 升级为 `session-memory-v2`。
- 回归：覆盖工具派生股票及交易日期进入记忆、订单号 `123456` 不进入股票列表、LLM 禁用 fallback、滚动窗口和提示注入 run 排除。

## BC-046：未使用的流式 Sanitizer 与校验后正则改写长期保留（2026-09-13）

- 发现方式：生产引用审查。`AgentAnswerStreamSanitizer` 只有单元测试消费者，当前 SSE 从不发布未校验 answer delta；最终 `AgentChatResponse` 仍通过多组正则改写内部工具名，发生在 Claim Ledger 和合规 gate 之后。
- 根因：旧流式回答阶段退役后，配套分段缓冲器和自然语言句式替换没有同步删除；输出边界同时保留“校验前禁止泄漏”和“校验后重写正文”两套策略。
- 修复：删除无生产消费者的流式 Sanitizer 及专属测试。最终输出只对公开契约中的精确内部标识执行字面量业务标签替换，不再识别或改写开放式句型；Prompt 泄漏仍在发布前 gate 失败关闭。
- 回归：覆盖反引号和普通精确工具标识替换、trace 名称不变、全部注册工具都有业务标签，以及股票链接仍只来自结构化工具事实。

## BC-047：Claim Ledger 可见路径与校验路径冲突导致正确答案失败（2026-09-13）

- 发现方式：Chrome 真实问答回归。包括“查询2026-09-11的涨停数量”在内的简单问题中，业务工具正确返回 `matched_count=40`，模型也正确生成答案，但最终状态仍为 `partial/validation_failed`。
- 根因：模型观察到的证据被包装为 `metadata` 和 `rows`，校验器却只接受原始 payload 路径；同时 gate 强制 claim statement 逐字出现在正文，空格或补充日期说明也会误判。第一次文本误判耗掉唯一修复机会，随后可见路径再次被拒绝。纯计数 payload 没有行集合，也被 `matched_count > len(rows)` 错判为来源截断。
- 修复：EvidenceStore 同时解析原始 payload 路径和模型实际可见的 `metadata`/`rows`/稳定状态字段类型化路径；Claim Ledger 保留标量值、证据域和引用一致性校验，但 statement 改为结构化摘要，不再与展示正文做逐字耦合；`result_mode=count` 不再依据空的展示行集合推断截断。证据版本升级为 `react-evidence-v4`，运行版本升级为 `react-runtime-v10`。
- 回归：覆盖真实的 count-only payload（含空 `items`）保持 `ok`、`metadata.matched_count`、`result_state` 与 `rows[0].symbol` 可解析，以及正文和 claim 仅格式不同仍能一次完成；错误标量值仍由原有测试拒绝。

## BC-048：最终证据校验器持续误伤正常问答（2026-09-13）

- 发现方式：修复 BC-047 后扩大审查生产拒绝分支与最近 300 次运行记录。最终 gate 仍会因无 evidence、历史 evidence、requirement 引用、missing/status 组合、Claim Ledger 路径或数值差异拒绝模型答案；普通文本回答还会因未调用 `finish` 被强制返工。这些条件共用一次修复预算，任意两个不一致即可把已有正确工具结果降级为 `validation_failed`。
- 用户决策：移除最终答案的 Evidence/Claim Validator，优先保证 Agent 能交付工具查询后的正常回答。输入安全审查、Prompt 泄漏保护、投资合规 Critic、工具参数 Schema、工具调用预算和确定性 `compute_result` 仍然保留，不属于本次删除范围。
- 修复：删除 Claim Ledger 类型和 finish schema 字段；删除 evidence ID、历史 scope、requirement、missing/status、路径、标量值之间的最终拒绝链；`update_task` 不再校验 evidence；模型直接返回普通文本时直接进入发布流程，不再因缺少类型化 `finish` 消耗修复次数。旧 checkpoint 中残留的 `claims` 字段兼容忽略。运行版本升级为 `react-runtime-v11`。
- 回归：覆盖无 evidence 的 complete、普通文本直接完成、历史/未知 evidence ID 不触发拒绝、错误 claim 值不触发 gate、旧 claims 字段兼容忽略，以及安全和投资合规审查继续执行。

## BC-049：普通文本回退把澄清与拒答终态硬编码为 complete（2026-09-15）

- 发现方式：Local30 正式基线中的 OFF-035 与 OFF-036。两条回答正文分别正确追问缺失股票、拒绝标的/仓位/收益承诺，但结构化 `task_status` 都是 `complete`，导致终态准确率只有 28/30。
- 根因：ReAct 模型未调用 `finish` 而直接返回正文时，运行时绕过类型化终态协议并无条件构造 `status=complete`。因此正文语义无论是完成、澄清还是拒答都会丢失真实终态。
- 修复：普通文本仅作为正文草稿保留，下一轮要求模型单独调用类型化 `finish` 并明确提交真实状态；后端不使用正文正则推断状态，也不针对评测题硬编码。运行版本升级为 `react-runtime-v12`。
- 回归：同一条普通文本回退路径分别覆盖 `complete`、`clarify`、`refuse`，断言必须经过第二次结构化提交且最终状态与提交一致；输入安全、投资合规和最终正文校验继续执行。

## BC-050：Frozen World 把生产等价的涨停状态参数误判为录制缺口（2026-09-15）

- 发现方式：修复终态后首次重跑 Local30，OFF-042 已成功取得创业板首板证据，随后用省略 `board_height`、显式 `closed_only=true` 的同范围查询交叉核验。World 已录制省略 `closed_only` 的等价路线，但 Frozen Registry 返回 `FrozenFixtureError`，整题被标为 `fixture_failure`。
- 根因：冻结匹配只比较公开参数的字面 canonical dict；生产 `limit_up_events` 会把省略状态和 `closed_only=true` 都归一为 `event_status=closed`，并为排序补 `board_height/desc`，冻结层没有复用这些行为等价关系。
- 修复：保留精确签名优先匹配，另建立仅覆盖生产已定义别名与默认值的语义签名；统一 `closed_only`、`broken_only`、`event_status` 以及默认排序。只有等价签名的 Observation 完全相同时才允许复用，冲突录制在 World 初始化时失败，不用空结果兜底。
- 回归：验证省略状态与显式收盘状态、默认排序与显式排序可命中同一真实录制；既有参数漂移、重复冲突、无录制工具和 Profile 拒绝测试继续通过。Frozen Registry 定向测试 20 项通过。

## BC-051：完整答案越过用户字段范围补全脏数据，裁判混淆并列指标口径（2026-09-15）

- 发现方式：Local30 P0 首轮完整答案审查。LH-034 只要求代码和名称，回答却主动增加行业与题材，并把证据中的截断值“计算机设”凭常识补成“计算机设备”；OFF-032 同时准确报告 `unsealed_count/unsealed_rate` 与 `failed_count`，裁判却因两组数值不同判定口径冲突。
- 根因：运行提示只要求事实来自工具，没有限制非必要字段扩张，也没有禁止修复源数据；完整答案裁判未明确区分可以并列出现的不同命名指标与真正的比例绑定错误。
- 修复：Agent 仅回答用户要求字段和必要口径，证据值原样使用，疑似脏数据显式标缺口，不自行补全；并列指标分别说明名称与口径。裁判新增“不同指标并列不冲突”规则，并以正向并列样本和错误比例绑定反例共同校准，不降低无证据事实的失败标准。运行版本升级为 `react-runtime-v13`。
- 回归：完整答案校准集同时覆盖受支持并列指标与错误指标绑定；运行时版本断言同步更新。最终 Local30 结果由新的正式复跑与完整答案审查确认。

## BC-052：Frozen World 漏录合法跨工具复核路线，稳定性轮次不可评分（2026-09-16）

- 发现方式：Local30 三轮稳定性实跑的第 3 轮。OFF-046 已通过 `limit_up_events` 得到正确科创板封板名单，但模型随后使用生产公开工具 `market_event_pool` 的同范围列表查询，并尝试 `event_status=all` 的更宽集合复核；两条合法路线均未录制，Supervisor 因 `fixture_failure` 将该轮标为不可评分。
- 根因：候选题主路线及常见默认参数已覆盖，但 recipe-level alternate routes 没有包含科创板在两个公开工具之间的交叉核验，也没有包含先取全部状态再按字段过滤的合法宽查询。冻结环境的封闭性因此把正常规划波动暴露为夹具缺口。
- 修复：为 2026-09-08 科创板增加 `market_event_pool(limit_up/list)` 和 `limit_up_events(event_status=all)` 两条真实录制路线；新增严格的 additive World 迁移，只允许 Case 契约和 World 元数据不变、旧录制及 Observation 全量保留、新录制纯增加，并要求旧技术验收路线在新 World 中继续成功。失败的原始轮次保留，不计入新基线稳定性。
- 回归：覆盖非增量变更拒绝、旧验收绑定迁移及新 Active Golden 单题 World 替换；新基线从零重新累计三轮，不混用迁移前结果。

## BC-053：正式裁判单次结构化参数畸形导致整轮报告中断（2026-09-16）

- 发现方式：迁移后 Local30 稳定性第 1 轮。30 个 Agent Case 全部完成且无基础设施失败，但完整答案裁判第 3 次调用返回无法解码的 tool arguments，`NativeFunctionCallingError` 直接中断整份正式报告。
- 根因：正式裁判对 Provider 的偶发结构化协议解码失败没有受限恢复；报告是串行聚合，单次协议瞬断会丢失后续 27 题的裁决。该错误不同于有效裁决为 fail 或 needs_review，不应改变业务标签。
- 修复：正式语义/完整答案裁判仅对 `NativeFunctionCallingError` 原题重试一次，GuardedProvider 继续逐次保存 request/response/error 并计入模型调用与 token；预算上限同步容纳最坏的一次重试。任何语义校验错误、有效 fail、第二次协议错误仍原样失败，不做多轮洗结果。
- 回归：首轮协议畸形、次轮成功时返回结果且调用数为2；普通 `ValueError` 不重试。原始中断目录保留，正式稳定性报告使用明确的新目录生成。

## BC-054：单指标问题主动扩展相邻统计，引入指标口径歧义（2026-09-16）

- 发现方式：迁移后 Local30 稳定性第 1 轮完整答案审查。OFF-032 只要求“最新本地交易日的收盘涨停家数和日期”，回答在正确给出40家后，又主动补充首板、连板、最高板及“炸板/未回封39家，未回封率31.03%”；斜杠合并了两个不同源指标，导致比例归属不清并被裁判正确判 fail。
- 根因：运行提示虽要求仅回答用户字段，但同时允许“必要口径”，模型把相邻市场综述误当成有用补充；不同指标的名称约束不足以稳定压过扩写倾向。
- 修复：明确最小充分回答规则：单指标问题只输出该指标、实际数据日期、必要口径与来源，不附加其他市场统计；名单题只给用户要求字段。不是靠问题正则或 Case 特判裁剪答案。运行版本升级为 `react-runtime-v14`。
- 回归：运行版本断言同步更新；新的 Local30 稳定性计数从该版本重新开始，旧轮次保留但不混入稳定面板。

## BC-055：真实空集 Case 漏录同筛选条件的 count 路线（2026-09-16）

- 发现方式：`react-runtime-v14` 稳定性第 3 轮。OFF-033 已通过 `market_event_pool(list)` 和 `limit_up_events` 两条证据确认 2026-09-11 科创板收盘涨停为空，随后用同工具同条件的 `result_mode=count` 复核时触发 `FrozenFixtureError`；回答事实与终态正确，但整题按 `fixture_failure` 不可评分。
- 根因：空集技术验收覆盖 list 的 limit 30/100 与另一个工具，却未录制同一公开工具的 count 模式。真实模型在多轮中偶发选择数量复核，冻结 World 对这条合法只读路线不完备。
- 修复：新增科创板收盘涨停 count 的真实录制；additive World 迁移同时支持批量 selection acceptance 与单题 empty acceptance，并逐条重放既有验收路线后才迁移 baseline digest。失败轮次继续保留且不计入稳定面板。
- 回归：新候选 World 必须包含全部旧录制且 Observation 不变；OFF-033 原有三条空集验收路线在新 World 全部成功。稳定计数在新 Active Golden 上重新开始。

## BC-056：按单条失败路线补录仍遗漏相邻市场对照查询（2026-09-16）

- 发现方式：OFF-033 补入科创板 count 后的下一轮实跑。模型依次完成科创板 list、`limit_up_events`、全市场 count、科创板 count 核对，随后查询创业板 count 作为非空对照；该合法查询未录制，再次触发 `fixture_failure`。
- 根因：冻结数据集按已见轨迹追加个别 alternate route，未对公开工具的离散参数空间形成闭包；模型的合理交叉核验会在 `market × result_mode × limit` 组合间波动，逐条追补无法成为稳定基线。
- 修复：数据集生成器对每个评测日期统一录制沪深主板、创业板、科创板的涨停 `count/list × limit 30/100` 完整组合；精简 OFF-033 World 仍只按显式清单迁入该题可能使用的 2026-09-11 市场对照路线，不把整套大 World 无审计复制进去。
- 回归：候选数据集构建继续真实调用生产 Gateway 并执行 record/replay；迁移继续要求旧录制 Observation 不变、原验收路线全量重放。此前失败轮次保留且不进入最终三轮面板。

## BC-057：连续 additive World 迁移复用 recording ID，资产写出后才加载失败（2026-09-16）

- 发现方式：路线闭包迁移后的首轮实跑。OFF-033 Worker 在生成任何请求前以 Pydantic `ValidationError` 退出；检查发现前一次和本次迁移都从 `additive-route-001` 编号，World 中存在重复 recording ID。
- 根因：迁移器用本批次位置生成 ID，只保证单次 additions 内唯一，没有对已有增量录制保持全局唯一；`model_copy(update=...)` 不重新执行 Pydantic validator，错误直到下次从 JSON 加载才暴露。
- 修复：新增 recording ID 改为工具名与规范化参数摘要派生；拼装后立即通过 `WorldSpec.model_validate` 重新验证完整资产，再执行 Frozen 初始化与旧路线重放。失败资产保留但不用于正式轮次。
- 回归：迁移写出前即拒绝重复 ID；合法连续迁移产生稳定唯一 ID，并能由标准 loader 重新加载。
