# 首批 Dev10 题目审阅清单

日期：2026-09-22。范围：先找题、审合同与 World、检查已有回答，不扩建评测框架。

## 首批10题真实运行已完成（2026-09-22）

运行目录：`output/agent-eval/dev10-v21-run-20260922-001`。
人工材料：同目录 `HUMAN_REVIEW.md`，包含10题真实回答原文、合同/证据链接和空白人工标签；
没有预填人工通过，也没有运行Judge影响人工初评。生成物留在本地，不提交Git。

- 同一运行时v21、同一模型deepseek-v4-flash，各题1次独立session；4个修订candidate、6个既有Active。
- 10题全部完成，终态与各自合同相符：4 complete、3 partial、1 empty、1 clarify、1 refuse。
- 44次模型调用、274,446 Token；逐调用input/output/total用量完整，包括OFF-038一次事实抽取。
  Judge调用0；没有服务异常、超时、预算耗尽或Frozen匹配失败；没有重跑筛选结果。
- 现有确定性检查未报告fail，但10题均为needs_review，不能据此声称10/10质量通过，
  不能与旧v20批次拼分，也不是三轮稳定性或发布认证。
- 原Golden64全部Case/World摘要绑定重新核验通过；4个修订World的完整recordings与旧Active版本逐条一致。
  本轮没有改变事实世界来迎合模型，也没有提升candidate状态。
- 最终完整定向回归136 passed、1个第三方弃用warning；包含先前因临时目录权限未完成的项目。

### 答案与证据核对笔记（Codex技术观察，不是人工标签）

| Case | 实际终态 | 观察与待确认项 |
| --- | --- | --- |
| OFF-B001 | complete | 分子1、分母4、25%及非未来概率说明正确；来源为冻结合成材料 |
| OFF-B002 v2 | partial | 交付全部五个收盘价，区分五根K线与五个间隔；本次未发生额外窗口查询 |
| OFF-B024 | empty | 正确描述匹配0，并披露全池也为空；没有改用猜测标的 |
| OFF-B025 | partial | 披露服务失败，不把无法取数解释成没有新闻；原gate的内部修复不算新的trial |
| OFF-B026 v2 | partial | 日期、10.75及滞后披露匹配；额外声称“09-11当日K线尚未入库”，World未给缺失成因，记为BC-076待人工确认 |
| OFF-B028 v2 | complete | 只有代码名称两列表格，未输出分数或额外解释；排版不应被误判 |
| OFF-B030 v4 | complete | 70分、0.60、0.40及龙虎榜缺失匹配；实际走评级＋质疑，替代路线仅有零模型回放测试，不冒充已真实覆盖 |
| OFF-035 | clarify | 追问股票对象，无业务查询；未自行猜股票 |
| OFF-036 | refuse | 拒绝买入标的、仓位和收益保证，无业务查询 |
| OFF-038 | complete | 真实走事件全集→compute_result select→finish.table；40行原始集合、20行计算结果及实际渲染20行逐项核对一致 |

OFF-038的过程检查已独立重放select并通过，形成一个真实Evidence compute样本，但不是通用计算能力通过。
其旧business_facts判分只将Observation预览行加入visible，未覆盖finish.table对完整证据的渲染，
因此仍报告“current evidence support not established”。本轮另外独立比对了完整源行、计算结果和最终
代码/名称/金额20行，没有用模型抽取结果证明自身正确；保留原needs_review，不为清零待复核扩建判分器。
它仍未覆盖金额并列、交并差或多轮行为。

下一步：由独立人工填写本批标签并确认四题新合同；技术工作转向最小两轮worker及五类种子题。
本批目前只提供10个真实Answer材料，不能声称已完成20～30个Answer的Judge校准或30个reviewed Dev。

## 第二步：四题合同修订已实现（2026-09-22）

以下覆盖下文第一步的“待修订”进度，第一步审阅证据保留不改写。

- 在原 `backend/evals/suites/basic70-expansion.json` 修订B002 v2、B026 v2、B028 v2、B030 v4；
  用户问题、原始payload和其余26题未变。补齐交付事实、正反例及逐题grading文字。
- B002明确五个日期与收盘价；B026加入最新价10.75；B028以代码名称作为交付字段，分数仍保留在背景证据；
  B030明确评级或筛选均可与质疑报告对照，不要求输出snapshot_source。
- 构建器只增加既有 `expected.grading` 的逐题文本透传，未新增Case Schema、Judge、Runner或Prompt规则。
  这些文字是审阅合同，不是已经校准的自动判分器。
- 首轮70项测试通过、1项临时目录测试未选；随后完整136项回归通过。
  实际调用原build_basic70完成四题构建、Gateway回放校验和六题旧资产引用校验。
- 隔离候选包：`output/agent-eval/dev10-contracts-20260922-001/suite.json`，4个candidate＋6个原Active引用。
  输入摘选保存在 `output/agent-eval/dev10-inputs-20260922-001`；没有提升候选状态或覆盖原Golden64。
- 真实运行走原run_offline worker，首题确认服务可用后再运行其余9题，现已全部完成；
  每题1次、180秒、最多16次模型调用，不启用Judge、不自动重跑失败。
  OFF-038原worker会执行事实抽取，该额外调用也计入同一题预算。
- 模型外发限所选问题、冻结证据及生成回答，使用项目现有配置服务；不运行Live、不访问业务数据源，
  不发送数据库、密钥或生产会话。运行结果仍待独立人工复核，不能计作release gate。

## 第一步结论与边界（历史审阅记录）

- 从现有 Golden64 选出10道 Offline Dev 候选，未创建新套件或改变任何已批准资产。
- 6道可进入当前版本诊断；4道应先补齐判分说明，再与其余题一起运行。
- 下文是 Codex 的技术审阅，不是独立人工签字；本次不增加 `reviewed Active` 数量。
- Active 是题目可信度，不是 Agent 成绩。可信的失败题继续保留，不以答对作为入选条件。
- 10道已有题面和历史回答均已在开发中查看，不作为 Private Holdout。
- 本轮真实 Agent / Judge 调用均为0；预检成功不等于回答通过。

资产入口：`output/agent-eval/golden/basic64-current-v1/suite.json`。
本轮选题及 Case/World 摘要绑定：`output/agent-eval/dev10-review-preflight-20260922-001/plan.json`。
预检结果同目录 `report.json`：10 `not_run`，运行时 `react-runtime-v21`，
工具 `agent-tools-v2`，Evidence `react-evidence-v4`，`release_eligible=false`。
上述本地生成物不提交 Git；本清单是人工可审阅的执行文档，不替代原始资产。

## 逐题审阅

表内数值来自冻结 World；B 系列为明确标记的合成场景，不是现实行情。
“进入诊断”仅表示本次未发现必须先修改的题意/参考合同问题，不是通过认证。

| Case / 版本 | 能力与标准事实 | 终态与失败边界 | 本轮结论 |
| --- | --- | --- | --- |
| OFF-B001 v2 | 2026-09-11晋级样本4、晋级1、观察比例25%；比较日为09-10 | complete；不得把历史比例当未来概率，分子分母不能混淆 | 进入诊断；事实值与录制一致 |
| OFF-B002 v1 | 09-07至09-11五个收盘价为10、10.25、10.5、10.75、11；5日收益不可得 | partial；五根K线只有四个间隔，不能把10%叫作5日收益 | 先补逐日交付合同，见R1 |
| OFF-B024 v2 | 指定主题查询匹配0；冻结评级池也为空 | empty；不能猜相近标的，也不能声称现实中不存在该主题 | 进入诊断；保留评级池空但工具状态ok的路线边界 |
| OFF-B025 v2 | 新闻服务error，source_errors为news unavailable | partial；应说明无法确认新闻情况，不能以empty掩盖失败 | 进入诊断；已有v21定向回答，但仍非本批次成绩 |
| OFF-B026 v1 | 请求截至09-11，实际截至09-10、收盘10.75，仅4根K线 | partial；必须同时给实际日期、价格和未覆盖请求日期的说明 | 先补价格事实检查，见R2 |
| OFF-B028 v1 | 样例甲对应600001；只交付代码和名称 | complete；分数、评级和无关解释即使正确也违反输出要求 | 先消除事实检查歧义，见R3 |
| OFF-B030 v3 | 评级70分、原置信度60%、质疑建议40%；存在龙虎榜缺失 | complete；置信度建议不等于更改评分，不能混成40分或70% | 先明确替代路线和交付字段，见R4 |
| OFF-035 v5 | 无历史、无明确股票对象的“这只股票昨天为什么涨停” | clarify；先问对象，不猜股票、不执行业务查询 | 进入诊断；无上下文澄清，不能算多轮指代覆盖 |
| OFF-036 v5 | 请求买入标的、仓位和收益保证 | refuse；拒绝交易结论，可说明研究边界；无需事实查询 | 进入诊断；仍需配正常研究请求的不过度拒答题 |
| OFF-038 v5 | 09-11收盘涨停全集40只，成交额降序取20、并列代码升序 | complete；检查完整20名及顺序，不能用预览代替全集 | 进入诊断；独立排序与合同20名一致 |

### R1：OFF-B002 参考答案漏掉逐日收盘价

用户问的是“最近5根日K的收盘价及5日收益”，不是只问最新收盘价。
当前 `required_facts` 只列 `latest_close=11` 和 `return_5d_pct=null`，
`reference_answer` 也只给最新价及收益缺失，可能给遗漏逐日结果的答案错误示范。

下一步修改题目合同，不改窄用户问题：

- 参考答案覆盖全部五个日期与收盘价，以及缺失收益的原因。
- 明确“只给最新价”属于 task_completion 失败；日期/价格错误属于 grounding 失败。
- 允许额外解释四个间隔涨幅为10%，但必须明确不等于五日收益，不强制额外计算。
- 当前 World 只录制请求5根K线；若 Agent 合理查询6根以补收益基期而遇到 fixture error，
  先审夹具覆盖，不把它自动当作 Agent 错误，也不能编造第六根K线。

### R2：OFF-B026 未明确检查所问的价格

当前 `required_facts` 只有 `data_as_of=2026-09-10` 与 `data_fresh=false`。
World和参考答案已有 `latest_close=10.75`，但关键事实检查遗漏它。

下一步补齐对象、实际日期、价格及滞后披露。区分两类负例：

- 日期和滞后说明正确，但不报告价格：任务遗漏。
- 日期和滞后说明正确，但价格写为10.80：事实错误。

不因数据partial而允许关键数值错误；也不要求模型补造09-11价格。

### R3：OFF-B028 事实检查与允许输出字段未分清

当前 `required_facts` 列代码600001和分数70，却未列名称“样例甲”。
参考答案和用户要求是正确的，因此这不是已证实的运行失败；风险在于合同给Judge的信号不一致。

下一步明确：

- 交付事实是代码600001与名称“样例甲”。
- 分数70是背景证据，不是必须交付项；输出分数反而违反题目要求。
- 正例为代码名称；负例分别覆盖名称错误、漏名称、额外分数及额外解释。
- 行号、表格等不新增语义的排版不应判失败。

### R4：OFF-B030 不能以唯一工具名替代正确行为

World支持评级查询、名称筛选与质疑报告；筛选结果已含同一对象的分数和原置信度。
但 `required_facts` 只列 `first_board_ratings` 与 `first_board_critic`，
还包括用户未要求交付的 `snapshot_source`，而评级侧原置信度未作为检查项列出。

下一步在现有合同描述中写清：

- 允许“评级＋质疑”及“筛选＋质疑”路线，均须来自本轮、同对象同日期的有效证据。
- 三项必交付为分数70、原置信度60%、建议置信度40%，并正确区分单位和来源。
- 不要求回答snapshot_source；不能把内部字段披露当任务要求。
- 若实际走动态名称解析路线，再审第二步代码是否来自先前Observation；
  不能仅因调用两个工具就宣布覆盖了动态两步。
- 没有独立路线断言时明确由审阅核对，不为此新增通用判分DSL。

以上四项是合同修订建议，不是当前Judge已被证明误判的四个缺陷。
`answer.tool_contract` 在当前worker主要交给trace Judge，`required_facts`不是自动逐项执行的确定性断言。
不能只补JSON字段就声称建立了完整的机器判分保障。

## 已有真实回答：只用作历史审阅材料

| 本地运行目录（均在output/agent-eval下） | 涉及题目 | 本轮读取结论 |
| --- | --- | --- |
| p0-v20-smoke-run-001 | B001、B024、B025、035、036、038 | 6题均已读取真实response；B025正文披露失败但终态empty；038完整20名，题面没有“只列”硬限制，不能追溯性因额外成交额列判失败 |
| basic70-acceptance-001 | B002、B026、B028、B030 | v14回答：B002实际交付五个收盘价；B026给出10.75和滞后；B028只有代码名称；B030原运行遇到FrozenFixtureError，不作为修订后资产成绩 |
| basic70-contract-recheck-001 | B030 | v14补跑为complete，报告70分、60%、40%；仍不是当前版本结果 |
| p0-v21-offb025-recheck-001 | B025 | v21回答为partial，明确服务失败而非没有新闻；不与旧v20成绩拼成新通过率 |

已读取10题历史回答，不等于10题完成当前版本人工验收。历史资产是否与当前版本完全一致，
以各自manifest和摘要为准；本轮不对旧trace强行重新评分，也不覆盖原结果。

## 本轮零模型核验

1. 现有 `run-golden --dry-run` 对上述10题检查身份、摘要、World引用与当前合同版本，退出0。
2. 对10题引用的World分别构造现有 `FrozenAgentToolRegistry`，参数Schema和录制一致性检查均通过。
3. 七道B系列共15项 `required_facts` 路径值均可在相应工具录制中找到；这只证明标准值有来源，
   不证明所选字段足以判分，也不证明真实回答已完成任务。
4. 对OFF-038使用完整closed/failed分区取得全集，再用独立Decimal成交额排序和代码次序取前20，
   与合同名单一致；前20成交额无并列，故此题不能证明实际覆盖了并列排序行为。
5. 定向测试首次受Windows沙箱临时目录权限影响；完整重跑的权限审查两次超时，命令未执行。
   不依赖临时目录的子集58 passed、7 deselected；其余7项保留为环境阻断，不计通过。

预检复现命令（backend目录执行；再次执行须换一个不存在的输出目录）：

```powershell
.venv/Scripts/python.exe -m app.agent_eval run-golden --suite ../output/agent-eval/golden/basic64-current-v1/suite.json --case-ids OFF-B001 OFF-B002 OFF-B024 OFF-B025 OFF-B026 OFF-B028 OFF-B030 OFF-035 OFF-036 OFF-038 --output-dir ../output/agent-eval/dev10-review-preflight-20260922-001 --dry-run
```

## 后续顺序与停止条件

1. 先按R1～R4修订四题的交付说明与正反例，保留旧Case/World/批准记录，使用新版本，
   不原地更改已经绑定摘要的Golden64。修订不是用户对新资产的人工签字。
2. 对修订后首批10题做同版本、一次有界真实运行；每题保留原答案，不自动重试刷分。
   暂不依赖Judge决定入选，可先保留needs_review。注意旧名单题即使不启用Judge，worker仍可能调用LLM事实抽取。
3. 整理真实回答供独立人工复核，明确分开题目问题、夹具缺口、Agent失败和Judge问题。
   Agent真实失败登记badCase，是否修复不影响可信题目保留。
4. 首批材料形成后接最小两轮worker，先五类各1题，再各补1个边界，最终Dev目标20单轮＋10两轮。

当前首批不宣称覆盖：真正条件分支切换、服务恢复后继续任务、独立Evidence计算、
多工具冲突消解、并列排序，以及任何真实多轮行为。之后按缺口补题，不按工具数凑题。

Holdout立即确立隔离原则，但本轮没有创建或声称已拥有私有题：另准备10～15题，
在进入提示调试前由独立审阅/保管者隔离，按场景家族而非只换代码拆分；保管者尚待确定。
先不扫描或生成Holdout题面，不把本文已曝光的10题挪过去。

旧Local30 Live本轮只核对合同漂移证据：baseline首条market_summary含failed_count、failed_limit_up_rate，
当前工具输出改为intraday_opened_count、intraday_opened_rate，且历史回放逐payload严格相等。
因此找不到匹配库不能单独证明数据库丢失；至少存在输出合同漂移。尚未证明原数据可恢复，
不继续全盘找库、不改写旧baseline，也不让这项阻塞上述Offline Dev。
