# Agent 评测近期执行优先级

历史计划说明（2026-09-23）：下文保留9月22日阶段计划与执行记录，其中两轮执行、Dev28人审、10题稳定性和Judge对照已有后续成果。当前状态见[建设清单](Agent_Evaluation_Real_Golden.md)，新的工作顺序见[评测集导航与后续计划](Agent_Evaluation_Guide.md)；不再将下文未回写的待办视为当前缺口。

> 2026-09-22 起执行。原则：停止扩建 Eval framework，优先增加真正经过人工复核的题目与真实 Agent 运行证据。

## 模型调用授权与执行边界（2026-09-22）

用户已明确：“授权，后续模型调用不用询问，统一允许”。后续本项目评测所需模型调用，
沿用已确认的DeepSeek目的地与题目、提示、工具定义、冻结市场证据范围，不再逐次申请确认。
更换外发目的地、扩大敏感数据范围或与本项目无关的调用不据此推定授权。
授权不等于无限重试：每轮仍须先固定题目、次数和预算，保留全部结果，不重跑挑选通过，不自动晋升Active。
前一轮Q01/Q08各1次，随后LH-R01在v22/v23各1次历史诊断；均180秒/题、最多16次模型调用/题、不启用Judge。
最新M02仅1段两轮会话，整段360秒/32次调用上限，实际10次调用、48,501 Token；无自动重跑、无Judge。
随后M04在v23诊断及v24修复后各1段，均360秒/32次上限，实际分别7/8次调用、23,058/36,558 Token；
原失败保留，无Judge，无同版本反复采样。

## 最新选题约束：真实任务与真实数据

用户已明确拒绝以“样例甲”等合成字段题代表真实产品能力。主评测要求真实股票名称/代码/日期及可追溯数据，
不能仅更换名称继续套合成数值。工程合成题保留原资产，但不计入本次真实场景Active目标。
重新设计的[10道单轮＋5道多轮题面](Agent_Evaluation_Real_Questions.md)已结合只读采集记录核实，
其中6道事件题已由用户bibo逐题认可；真实计数冲突（103被工具截断成100）已修复，108项定向测试通过。
58条工具录制已生成v2，仅两条相关观测更正命中数，业务合同未变；没有新增Active，也不修改真值或Prompt。
用户明确DeepSeek外发授权后，6题首轮运行已完成：4待答案人审、Q01缺录制不可评分、Q08发布校验失败。
该轮33次调用、294,613 Token，不启用Judge。后续Q08运行时边界已修复为v22，旧首次轨迹确定性回放交付13只；
Q01已观察缺口全部真实补录，v3包62条回放一致，核心回归186 passed、扩展回归664 passed/10 skipped；没有新增模型调用或Active。
用户已授权后续范围内模型调用；Q01/Q08各1次有界验证已完成，共14次调用、162,339 Token，不启用Judge。
Q08已真实交付13只且逐项核对一致，仍待人审；Q01因2种新未录制参数仍不可评分，另有BC-082措辞问题待复核。
后续已复用历史工具路径完成LH-R01单次诊断：62条基线一致、原缺口真实执行，无fixture阻塞；
5次调用、47,367 Token，却因模型额外声明完整103只名单、双表占位与100行截断而最终交付失败（BC-084）。
受限四工具历史模式与旧offline分开记账，61项回归通过、10项跳过，不计新增Active。
随后v23最小提示/契约说明修复已完成：同输入单次实跑5次调用、36,278 Token，首次finish交付核心统计，
complete/needs_review；扩展670 passed、10 skipped，残留技术观察见BC-086。
用户bibo随后对这份v23原答填写pass/pass/pass、意见“无”：尊重该答案人审通过，保留技术观察，
不推定其他题或所有手写名单/来源陈述一概通过，不再针对Q01继续改提示。其他Dev6原答仍待标签，不重跑挑答案。
M02题意及两轮原答已获bibo认可、三维pass，绑定审核原件和实际响应哈希，不再重复审核它。
M04日期修改追问已实测发现上下文丢失（BC-087），v24通用修复后同题同数据单次验证成功交付72/61只，
待新原答人审；696 passed、10 skipped。真实人审通过累计2份（Q01单轮、M02会话），正式Active迁移仍未执行。
一次complete不等于稳定性通过；当前优先补M01/M03并准备M05跨日边界，不继续针对Q01调提示或扩框架。
详见[真实Dev6合同与录制](Agent_Evaluation_Real_Dev6_Contracts.md)。先确认题目是否值得测，再跑模型；不修合同凑题。

## 统一口径

当前 Golden64 表示题目资产和判分合同曾获准进入套件，不等于64题都完成当前运行时下的人工答案复核。
首批执行记录见[Dev10题目审阅清单](Agent_Evaluation_Dev10_Review.md)：10题已定位，4题需先补合同说明；
技术审阅不冒充独立人工签字，可信失败题不因Agent尚未答对而剔除。
后续报告必须分别写出：

- `contract-admitted`：题目与合同已准入的资产库存；
- `reviewed Active`：问题、World、预期行为、真实回答和最终裁决均完成逐题复核；
- `Dev`：允许反复运行和定位问题；
- `Private Holdout`：不用于逐题调提示，只在阶段验收时运行。

不新增新的评测框架或平行 Runner 来表达这些口径；先用清单和现有资产状态管理，保留历史产物不覆盖。

## P0-A：扩充 reviewed Active

目标先达到40题，其中30题作为 Active Dev，10题作为 Private Holdout。当前10题代表运行只作为首批审阅材料，
不能按一次 Judge 结果自动晋升。

40题应覆盖以下能力；一题可以覆盖多个能力，但每类至少包含独立主场景和失败/边界场景：

| 能力 | 最低目标 | 重点检查 |
| --- | ---: | --- |
| simple factual | 6 | 对象、日期、指标、数值、来源 |
| dynamic two-step | 5 | 第二步必须依赖第一步 Observation |
| conditional branch | 4 | empty、命中、缺字段走不同合法路径 |
| partial/error recovery | 5 | error不伪装empty，成功部分不丢失 |
| multi-tool comparison | 4 | 口径分离、冲突与共同覆盖范围 |
| Evidence compute | 4 | 完整集合、排序、交并差及lineage |
| ambiguous query | 3 | 必要澄清，不擅自选对象或日期 |
| safety/refusal | 4 | 研究事实与交易指令边界 |
| answer output constraint | 5 | 只列字段、完整名单、必要缺失说明 |

逐题准入至少核对：用户要求、数据世界、合法替代路线、终态、关键事实、缺失披露、输出限制和安全边界。
真实回答失败应进入 bad case 并修复或保留失败，不能通过改宽 Case 合同晋升。

执行批次：

1. 盘点现有候选，选出覆盖缺口最小的首批10题；
2. 每批最多10题，完成真实运行、人工复核和必要修复后再开始下一批；
3. 从第一批起预留独立Holdout材料与保管边界，不能把全部题调试过后再拆分；Dev达到30题后冻结并阶段验收；
4. 不为追求40题降低证据、终态或复核标准。

## P0-B：最小 Multi-turn runner

2026-09-22执行状态：既有worker已支持1～2个真实user turn，拒绝预置assistant；生产上下文服务接续，
逐轮保存request/context/response/session，根目录调用账本覆盖整段。两消息尚不触发生产摘要，memory为null，未伪造摘要。
仅末轮合同可自动评分，暂不支持两轮Judge，首轮原答留给整段人审；首轮错误停止，不能拿首轮成绩代表追问。
M02已跑1次并获人审，验证结果集身份继承及同日重新取证。M04在v23失败后，v24已验证日期修改及筛选条件接续，
两轮原答待人审；回归696 passed、10 skipped。M01/M03未运行，不能因语义追问修复便宣称这两类已通过。
M05跨日时钟/逐轮数据可见范围仍需落实，不能只换答案或共享同一时间锚假称完成。

只扩展现有 Golden worker，使一个 Case 可以按顺序执行两轮真实 Agent：

```text
Turn 1 -> 真实 Agent -> 保存 assistant、结构化响应和 session context
Turn 2 -> 同一 session 继续 Agent -> 单独保存第二轮 trace、evidence 和终态
```

必须保证第二轮不会直接复用旧 evidence 作为当前事实；需要事实时重新查询，并能审计刷新行为。首批至少5题：

- previous entity；
- previous result set；
- pronoun；
- date modification；
- old evidence refresh。

验收重点是会话语义和证据新鲜度，不增加新的通用 Planner、Judge 或报告框架。

## P1-A：Dev / Holdout

- Active Dev：30题，可重复运行和定位问题；
- Private Holdout：10题起步，可扩至15题；题面、答案和关键断言不进入日常提示调试材料；
- Holdout失败可以触发归因和通用修复，但不得把该题改成新的提示特例后反复刷分；
- 每次报告同时给Dev与Holdout结果，禁止合并成一个通过率掩盖过拟合。

## P1-B：Stability

从关键能力中选择固定 Case，每题独立运行3次。至少输出：

- `pass@1`；
- `3/3 stable rate`；
- `2/3 rate`；
- `1/3 rate`；
- `0/3 rate`；
- 每题三次结果、失败原因和Token完整性。

`pass@1`使用预先指定的第一次运行；含unscorable或待复核的三轮单列为不完整结果，
不归为普通0/3、不静默剔除，同时显示判分覆盖率。不同trial不复用session。

先对 reviewed Active 的关键子集做稳定性，不对未复核资产批量烧模型额度。

## P1-C：Judge校准

准备20～30个真实 Answer，由人工先标注 `task_completion`、`grounding`、`boundary_safety`，再运行Judge。
报告逐维一致率、false pass、false fail、abstain和协议/Provider失败；人工标签与Judge输入都版本化。

Judge只有在样本完整、人工标签已确认且错误边界可接受后，才讨论进入 release gate。在此之前只作诊断，
不得通过重复调用筛选有利裁决，也不以合成作者标签代替人工标签。

## 明确暂停

- 暂停新增评测Schema、通用框架层、独立工具Runner和新的报告体系；
- 暂停Golden64全量真实运行；
- 暂停围绕单个可见题反复调Prompt；
- 暂停把未校准Judge结果用于发布通过；
- 旧Local30回放阻断先区分数据漂移与工具输出合同漂移；不以反向构造数据库阻塞上述Dev题扩充。
